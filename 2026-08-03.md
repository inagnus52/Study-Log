# 2026-08-03 월요일
## 오늘 배울 내용
**프로세스, 서비스, 패키지, 로그**
- 프로그램과 프로세스 차이
- PID
- 백그라운드 프로세스
ps, jobs, kill, killall, top, free -h, df -h, du, uptime, who

- 데몬과 서비스
- systemd 개념
- Nginx 설치
- 서비스 시작·중지·재시작
- 부팅 시 자동 실행 설정
- 브라우저에서 기본 페이지 확인
- 서비스 장애 확인 순서 정리
- `상태 확인 → 로그 확인 → 설정 확인 → 재시작`

## 오늘 배운 내용
### 프로그램과 프로세스
#### 프로그램과 프로세스의 차이
| to do                   | 의미                          |
| --------------------- | --------------------------- |
| `ps`                  | 지금 내 터미널에서 실행 중인 프로세스 확인    |
| `ps aux`              | 시스템 전체 프로세스 자세히 확인          |
| `ps aux \| grep bash` | 전체 프로세스 중 `bash`가 들어간 것만 검색 |
| `sleep 300 &`         | `sleep`이라는 프로세스를 백그라운드에서 실행 |
| `jobs`                | 현재 터미널의 백그라운드 작업 확인         |
| `fg %1`               | 백그라운드 작업을 다시 앞쪽으로 가져오기      |

<br>

- 프로그램(program): 디스크에 저장된 실행 파일
- 프로세스(process): 프로그램을 실제로 실행하고 있는 상태

```
설치되어 있는 프로그램이 메모리에 올라와 CPU와 메모리를 사용하는 프로세스가 됨
```
같은 프로그램을 여러 번 실행하면 각각 별도의 프로세스가 만들어짐

```
nano 프로그램 1개
├── nano 프로세스 1
├── nano 프로세스 2
└── nano 프로세스 3
```
각 프로세스를 구분하기 위해 리눅스는 PID(Process ID)라는 고유 번호를 부여함.

#### `ps` 명령어를 실행

```
PID   TTY      TIME       CMD
330   pts/0    00:00:00   bash
1666  pts/0    00:00:00   ps
```
- `PID`: 프로세스마다 부여되는 고유 번호
- `TTY`: 프로세스가 연결된 터미널
- `TIME`: 프로세스가 실제로 CPU를 사용한 누적 시간
- `CMD`: 실행 중인 명령어 또는 프로그램

- `bash` — 현재 사용 중인 셸 프로세스이며 PID는 330입니다.
- `ps` — 프로세스 목록을 확인하기 위해 방금 실행한 ps 자체이며 PID는 1666입니다.


![프로세스 실습(sleep)](/images/프로세스%20실습(sleep).png)
- `sleep 300`: 아무 작업 없이 300초(5분) 동안 기다리는 프로세스
- `&`: 명령어를 백그라운드에서 실행
- `[1]`: 현재 터미널에서 관리하는 작업 번호(Job ID)
- `1844`: 시스템 전체에서 프로세스를 구분하는 PID

-즉, `&`명령어는 프로세스를 백그라운드에서 실행하도록 하는 명령어이며, **어떤 프로세스는 앞에서 실행하고 어떤 프로세스는 뒤에서 실행할 수 있다**

**-`sleep`이 뒤에서 실행되는 동안 터미널에서는 다른 명령어를 계속 입력할 수 있습니다.**

<br>

#### 백그라운드 작업 확인
```
jobs
```

->결과:
```
parkseonga@DESKTOP-4CLG47Q:~$ jobs 
[1]+ Running            sleep 300 &
```
- `[1]`: 작업 번호(Job ID)
- `+`: `fg`나 `bg`에서 작업 번호를 생략했을 때 기본으로 선택되는 작업
- `Running`: 현재 백그라운드에서 실행 중
- `sleep 300 &`: 실행한 명령어

<br>
| 위치    | 뜻        | 터미널 상태             |
| ----- | -------- | ------------------ |
| 포그라운드 | 앞에서 실행 중 | 터미널을 그 명령어가 차지함    |
| 백그라운드 | 뒤에서 실행 중 | 터미널에서 다른 명령어 입력 가능 |

<br>

#### 백그라운드 작업을 다시 앞쪽으로 가져오기
```
fg %1
```
-이 명령어를 실행하면 `sleep 300`이라는 명령어가 포그라운드에서 실행 중이다. 
-`sleep 300` 명령어는 300초 동안 아무것도 안하고 기다리는 명령어라서 사용자가 보기에는 
```
아무것도 안 뜸
프롬프트도 안 보임
입력도 안 되는 것처럼 보임
```
처럼 느껴진다. 하지만 `sleep 300`이 포그라운드에서 실행 중인 상태이므로 
이 상태에서 선택지는 두 개이다:
```
ctrl+c -> 프로세스 종료
ctrl+z -> 프로세스 일시정지
```

<br>

#### 다시 백그라운드로 보내기
- `ctrl+z` 후  `bg %1` 하면 다시 백그라운드로 보낼 수 있음

<br>

#### 작업 번호로 프로세스 종료하기
```
kill %1
```
그 다음 확인:
```
jobs
```
아무 것도 안나오면 작업이 완전히 사라진 상태

<br>

#### PID로 프로세스를 종료하는 방식
- PID: Process ID의 줄임말로, 리눅스가 실행 중인 프로세스마다 붙여주는 고유 번호
-프로그램 이름이 같아도 여러 번 실행하면 PID는 각각 다르게 붙는다

```
sleep 600&
```
그러면 이런 식으로 나옴
```
[1] 2050
```
- `[1]: 작업 번호
- `2050`: PID

<br>

#### 프로세스 이름으로 종료하는 방식
```
killall
```
`kill`은 PID로 종료하고, `killall`은 프로세스 이름으로 종료함
```
kill all sleep
```
- 뜻: 이름이 `sleep`인 프로세스를 전부 종료해라.

<br>

#### 현재 실행 중인 프로세스 실시간으로 보기
```
top
(나올 때는 q)
```
- 실행 하면 CPU, 메모리 사용량이 높은 프로세스들이 계속 바뀌면서 보인다
- `top`: 현재 실행 중인 프로세스들을 실시간으로 보는 명령어
- `ps aux`: 현재 실행 중인 프로세스들을 한 번 찍어서 보여주는 사진 같은 명령어 
- 서버가 느릴 때 "누가 자원을 많이 먹고 있지?" 확인할 때 자주 사용

`top` 화면이 뜬 상태에서 아래 키를 눌러 **정렬 기준 바꾸기**
| 키   | 의미                |
| --- | ----------------- |
| `P` | CPU 사용률 높은 순으로 정렬 |
| `M` | 메모리 사용률 높은 순으로 정렬 |
| `T` | 실행 시간 오래된 순으로 정렬  |
| `q` | top 종료            |
-주의할 점은 `P`,`M`,`T`가 대문자이라는 것임

#### 메모리 상태 확인 명령어
```
free -h
```

결과는 대략 이런 모양입니다.

               total        used        free      shared  buff/cache   available
Mem:           3.7Gi       500Mi       3.1Gi        ...
Swap:          1.0Gi         0B        1.0Gi

여기서 중요한 건 주로 3개입니다.

|항목|뜻|
|total|전체 메모리|
|used|사용 중인 메모리|
|free|완전히 비어 있는 메모리|
|available|실제로 새 프로그램이 쓸 수 있는 메모리|

현재 내 노트북 메모리 상태:
```
Mem:   total 3.7Gi   used 492Mi   free 3.2Gi   buff/cache 127Mi   available 3.2Gi
Swap:  total 1.0Gi   used 0B      free 1.0Gi
```
| 항목           | 뜻                      | 지금 상태 |
| ------------ | ---------------------- | ----- |
| `total`      | 전체 메모리                 | 3.7Gi |
| `used`       | 사용 중인 메모리              | 492Mi |
| `free`       | 완전히 비어 있는 메모리          | 3.2Gi |
| `buff/cache` | 리눅스가 임시로 활용 중인 캐시      | 127Mi |
| `available`  | 실제로 새 프로그램이 쓸 수 있는 메모리 | 3.2Gi |
| `Swap used`  | 메모리 부족 시 디스크를 빌려 쓰는 공간 | 0B    |

#### 디스크 용량 확인 명령어
```
df -h
```
- `df`: disk free, 파일시스템의 디스크 사용량 확인
- `-h`: human readable, 사람이 읽기 쉬운 단위로 표시

내 노트북 디스크 용량 확인 결과:
```
/dev/sdd       1007G  1.7G  954G   1% /
C:\             460G  315G  146G  69% /mnt/c
```
해석하면 
```
/dev/sdd 1007G 중 1.7G 사용, 954G 남음
```
Ubuntu 쪽 공간은 거의 비어 있습니다. 사용률이 1%라서 매우 넉넉합니다.
```
C:\ 460G 중 315G 사용, 146G 남음
```
윈도우 C드라이브는 69% 사용 중이고, 146GB 정도 남아 있습니다. 아직 위험한 상태는 아니지만, C드라이브는 80~90% 넘어가면 정리하는 게 좋습니다.

#### 폴더 사용량 확인 명령어
```
du -sh ~
```
| 부분   | 의미                               |
| ---- | -------------------------------- |
| `du` | disk usage, 파일/폴더가 쓰는 용량 확인      |
| `-s` | summary, 전체 합계만 보기               |
| `-h` | human readable, 사람이 읽기 쉬운 단위로 보기 |
| `~`  | 내 홈 디렉터리                         |

내 폴더 확인 결과:
```
parkseonga@DESKTOP-4CLG47Q:~$ du -sh ~
152K    /home/parkseonga
```

#### 어떤 파일/폴더가 용량을 쓰는지 더 자세히 보는 명령어
```
du -sh ~/*
```
| 부분    | 의미               |
| ----- | ---------------- |
| `du`  | 용량 확인            |
| `-s`  | 합계만 보기           |
| `-h`  | 사람이 읽기 쉬운 단위     |
| `~/*` | 홈 폴더 안의 모든 파일/폴더 |


내 폴더 확인 결과:
```
du: cannot access 'sh': No such file or directory
4       /home/parkseonga/cloud-study/network
4       /home/parkseonga/cloud-study/python
4       /home/parkseonga/cloud-study/linux/practice
12      /home/parkseonga/cloud-study/linux
24      /home/parkseonga/cloud-study
4       /home/parkseonga/linux-practice
0       /home/parkseonga/permission_test.txt
4       /home/parkseonga/test.txt
```
-홈 폴더 안에서 제일 큰 건:
```
24      /home/parkseonga/cloud-study
```

#### 시스템이 켜진 뒤 얼마나 오래 실행 중인지 보여주는 명령어
```
uptime
```

내 노트북 결과:
```
16:59:51 up  3:12,  1 user,  load average: 0.00, 0.00, 0.00
```

| 항목                               | 뜻                   |
| -------------------------------- | ------------------- |
| `16:59:51`                       | 현재 시간               |
| `up 3:12`                        | 시스템이 켜진 지 3시간 12분 됨 |
| `1 user`                         | 현재 접속 중인 사용자 1명     |
| `load average: 0.00, 0.00, 0.00` | 시스템 부하가 거의 없음       |

load average의 숫자 3개는 각각 보통:
```
1분 평균, 5분 평균, 15분 평균
```
입니다. 지금은 전부 0.00이라서 CPU가 아주 한가한 상태예요.

#### 현재 로그인한 사용자를 확인하는 명령어
```
who
```

| 명령어      | 의미                   | 지금 상황              |
| -------- | -------------------- | ------------------ |
| `who`    | 현재 로그인 세션 확인         | WSL이라 비어 있을 수 있음   |
| `whoami` | 지금 명령어를 실행 중인 사용자 확인 | `parkseonga`가 나올 것 |
| `uptime` | 접속 사용자 수 포함한 시스템 상태  | 아까 `1 user`로 나왔음   |

#### 프로세스/시스템 상태 확인 명령어
| 명령어             | 뜻                           | 언제 쓰는지                    | 예시              |
| --------------- | --------------------------- | ------------------------- | --------------- |
| `ps`            | 현재 터미널에서 실행 중인 프로세스 확인      | 간단히 프로세스 볼 때              | `ps`            |
| `ps aux`        | 시스템 전체 프로세스 자세히 확인          | 전체 실행 목록 볼 때              | `ps aux`        |
| `sleep 600 &`   | 600초 동안 대기하는 프로세스를 백그라운드 실행 | 백그라운드 실습용                 | `sleep 600 &`   |
| `jobs`          | 현재 터미널의 백그라운드 작업 확인         | 작업 번호 확인할 때               | `jobs`          |
| `fg %1`         | 1번 작업을 앞으로 가져오기             | 백그라운드 작업을 다시 화면 앞으로 가져올 때 | `fg %1`         |
| `Ctrl + Z`      | 실행 중인 작업 일시정지               | 앞에서 실행 중인 작업을 멈출 때        | `Ctrl + Z`      |
| `bg %1`         | 일시정지된 1번 작업을 뒤에서 다시 실행      | 멈춘 작업을 백그라운드로 보낼 때        | `bg %1`         |
| `kill %1`       | 작업 번호 1번 종료                 | 현재 터미널의 작업 번호로 종료할 때      | `kill %1`       |
| `kill 2556`     | PID 2556 프로세스 종료            | PID로 특정 프로세스 종료할 때        | `kill 2556`     |
| `killall sleep` | 이름이 `sleep`인 프로세스 전부 종료     | 같은 이름의 프로세스를 한 번에 종료할 때   | `killall sleep` |
| `top`           | 프로세스와 CPU/메모리 상태 실시간 확인     | 서버가 느릴 때 상태 확인            | `top`           |
| `free -h`       | 메모리 사용량 확인                  | RAM/Swap 상태 볼 때           | `free -h`       |
| `df -h`         | 디스크 전체 용량 확인                | 파일시스템별 남은 공간 볼 때          | `df -h`         |
| `du -sh ~`      | 내 홈 폴더 전체 용량 확인             | 특정 폴더가 얼마나 큰지 볼 때         | `du -sh ~`      |
| `du -sh ~/*`    | 홈 폴더 안 항목별 용량 확인            | 어떤 폴더가 용량을 많이 쓰는지 볼 때     | `du -sh ~/*`    |
| `uptime`        | 시스템이 켜진 시간과 부하 확인           | 서버가 얼마나 오래 켜져 있었는지 볼 때    | `uptime`        |
| `who`           | 로그인한 사용자 확인                 | 현재 접속 세션 확인할 때            | `who`           |
| `whoami`        | 현재 명령어를 실행 중인 사용자 확인        | 내가 어떤 사용자로 작업 중인지 볼 때     | `whoami`        |

| 개념    | 뜻                              | 예시             |
| ----- | ------------------------------ | -------------- |
| 작업 번호 | 현재 터미널 안에서만 쓰는 번호              | `[1]`, `%1`    |
| PID   | 리눅스 전체에서 프로세스마다 붙는 고유 번호       | `2556`         |
| 백그라운드 | 명령어를 뒤에서 실행하는 상태               | `sleep 600 &`  |
| 포그라운드 | 명령어가 터미널 화면 앞에서 실행되는 상태        | `fg %1`        |
| Swap  | 메모리가 부족할 때 디스크를 임시 메모리처럼 쓰는 공간 | `free -h`에서 확인 |


### 로그 확인
- 서버에 무슨 일이 있었는지 기록을 읽는 법을 배움
| 주제           | 배우는 내용                                       |
| ------------ | -------------------------------------------- |
| 로그의 의미       | 시스템, 서비스, 로그인, 오류 기록이 어디에 남는지 이해             |
| `journalctl` | systemd 기반 시스템 로그 확인                         |
| 최근 로그 보기     | `journalctl -n 20`처럼 최근 몇 줄만 보기              |
| 실시간 로그 보기    | `journalctl -f`로 새로 생기는 로그를 계속 확인            |
| 서비스별 로그 보기   | `journalctl -u 서비스명`으로 특정 서비스 로그만 확인         |
| 부팅 로그 보기     | `journalctl -b`로 이번 부팅 이후 로그 확인              |
| 시간 기준 필터링    | `--since`, `--until`로 특정 시간대 로그 확인           |
| `/var/log`   | 리눅스 전통 로그 파일 위치 이해                           |
| `tail`       | 로그 파일 끝부분 확인                                 |
| `tail -f`    | 로그 파일을 실시간으로 따라가기                            |
| 인증 로그        | 로그인, sudo, SSH 접속 기록 확인                      |
| 에러 찾기        | `grep`, `journalctl -p err`로 오류 로그 찾기        |
| 서버 운영 관점     | “왜 접속이 안 됐는지”, “누가 로그인했는지”, “서비스가 왜 죽었는지” 추적 |

<br>

**대표 명령어**
| 명령어                       | 뜻                |
| ------------------------- | ---------------- |
| `journalctl -n 20`        | 최근 시스템 로그 20줄 보기 |
| `journalctl -f`           | 시스템 로그 실시간 보기    |
| `journalctl -b`           | 이번 부팅 이후 로그 보기   |
| `journalctl -p err`       | 에러 등급 로그 보기      |
| `journalctl -u ssh`       | SSH 서비스 로그 보기    |
| `ls /var/log`             | 로그 파일들이 있는 폴더 확인 |
| `tail /var/log/syslog`    | syslog 마지막 부분 보기 |
| `tail -f /var/log/syslog` | syslog 실시간 확인    |
| `grep error 파일명`          | 로그에서 error 단어 찾기 |

#### 로그 확인 기본 명령어
```
journalctl
```
최근 로그 n줄만 보는 법
```
journalctl -n 20
```
최근 로그 20줄만 보기

![로그 확인 실습](/images/로그%20확인%20실습.png)

긴 줄을 안 잘리게 보려면 
```
journalctl -n 20 --no-pager
```
또는 전체 줄을 더 자세히 보려면:
```
journalctl -n 20 -o cat
```
에러/경고 로그만 골라보는 명령어:
```
journalctl -p warning -n 10 --no-pager
```
- `-p warning`: warning 이상 등급 로그만 보기(`-p`는 priority, 즉 로그의 중요도/등급을 뜻함)
- `n 10`: 최근 10줄만 보기
- `--no-pager`: 화면 넘김 없이 바로 출력

- 로그 등급
| 등급        | 의미           |
| --------- | ------------ |
| `info`    | 일반 정보        |
| `notice`  | 참고할 만한 정보    |
| `warning` | 경고           |
| `err`     | 오류           |
| `crit`    | 심각한 오류       |
| `alert`   | 즉시 조치 필요     |
| `emerg`   | 시스템 사용 불가 수준 |

<br>

이번 부팅 이후 로그 보기
```
journalctl -b -n 20 --no-pager
```
- "이번에 WSL Ubuntu가 켜진 뒤 생긴 로그 중 최근 20줄을 보여줘"라는 뜻임
- `-b`: 이번 부팅 이후 로그만 보기(`b`: boot)

특정 서비스 로그만 골라보기
```
journalctl -u wsl-pro.service -n 10 --no-pager
```
- `-u`: unit, 즉 systemd 서비스 단위를 뜻한다

**/var/log 폴더 보기**
```
ls /var/log
```
- `/var/log`: 로그 파일들이 모여 있는 대표 폴더

- 로그 파일은 보통 아래쪽으로 갈수록 최신 기록이라서 `tail`로 마지막 10줄 부분부터 보는 게 기본임
```
tail /var/log/syslog
```

로그를 실시간으로 따라가는 명령어
```
tail -f /var/log/syslog
```

**인증(로그인/권한) 관련 로그**
```
tail /var/log/auth.log
```

**로그에서 단어 찾기**
```
grep error /var/log/syslog
```

### 서비스 관리
- **서비스**: 백그라운드에서 계속 돌아가는 프로그램
| 서비스                 | 하는 일                         |
| ------------------- | ---------------------------- |
| `ssh`               | 다른 컴퓨터에서 원격 접속할 수 있게 해줌      |
| `cron`              | 정해진 시간에 명령어를 자동 실행           |
| `networking`        | 네트워크 연결 관리                   |
| `nginx` / `apache2` | 웹사이트를 보여주는 웹 서버              |
| `mysql`             | 데이터베이스 서버                    |
| `wsl-pro.service`   | WSL에서 Ubuntu Pro 관련 기능 연결 시도 |

-실제 서버는 서비스들로 돌아간다

| 핵심                   | 의미               |
| -------------------- | ---------------- |
| 서비스                  | 뒤에서 계속 돌아가는 프로그램 |
| `systemctl`          | 서비스를 관리하는 명령어    |
| `status`             | 상태 확인            |
| `start`              | 서비스 시작           |
| `stop`               | 서비스 중지           |
| `restart`            | 서비스 재시작          |
| `enable`             | 부팅 시 자동 시작 설정    |
| `disable`            | 부팅 시 자동 시작 해제    |
| `journalctl -u 서비스명` | 특정 서비스 로그 보기     |


진짜 서버 운영에서 제일 많이 쓰는 건 이 5개입니다.
```
systemctl status 서비스명
sudo systemctl start 서비스명
sudo systemctl stop 서비스명
sudo systemctl restart 서비스명
journalctl -u 서비스명 -n 20 --no-pager
```

<br>

| 목표            | 명령어                                                              |
| ------------- | ---------------------------------------------------------------- |
| 서비스 목록 보기     | `systemctl list-units --type=service --no-pager`                 |
| 실행 중인 서비스만 보기 | `systemctl list-units --type=service --state=running --no-pager` |
| 특정 서비스 상태 보기  | `systemctl status wsl-pro.service`                               |
| 서비스 시작        | `sudo systemctl start 서비스명`                                      |
| 서비스 중지        | `sudo systemctl stop 서비스명`                                       |
| 서비스 재시작       | `sudo systemctl restart 서비스명`                                    |

#### 서비스 목록 보기
```
systemctl list-units --type=service --no-pager
```
![서비스 목록 보기 실습](/images/서비스%20목록%20확인%20실습.png)

