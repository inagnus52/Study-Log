# 2026-08-16 일요일
## 오늘 배울 내용
**가상화**
- 물리 서버
- Hypervisor
- Virtual Machine
- VM
- vCPU
- vRAM
- Virtual Disk

**Storage**
- HDD
- SSD
- Block Storage
- File Storage
- Object Storage
- Volume
- Snapshot
- Backup

## 오늘 배운 내용
### 가상화
#### Hypervisor(하이퍼바이저)
하나의 물리 서버를 여러 개의 가상 서버로 나누어 관리하는 관리자.
물리 서버 위에 올라간다.
```
┌─────────────────────────────┐
│        Physical Server      │
│                             │
│ CPU 32 Core / RAM 128 GB    │
└──────────────┬──────────────┘
               ↓
        ┌──────────────┐
        │  Hypervisor  │
        └──────┬───────┘
          ↓    ↓    ↓
        VM1   VM2   VM3
```

#### VM(Virtual Machine, 가상 머신)
물리 서버에 여러 VM을 만들 수 있다
각 VM은 자신이 독립적인 컴퓨터인 것처럼 동작한다

예를 들어 
```
CPU 32 Core
RAM 128 GB
```
가 있다면 Hypervisor가 이를 나눠서
```
VM1
CPU 4 Core
RAM 16 GB

VM2
CPU 8 Core
RAM 32 GB

VM3
CPU 4 Core
RAM 16 GB

...
```
VM1과 VM2는 독립적인 컴퓨터인 것처럼 작동하기 때문에 VM1에 Ubuntu를 설치할 수 있고, VM2에는 다른 프로그램을 설치할 수도 있음

#### vCPU와 vRAM
- vCPU: VM에게 CPU 자원을 할당해주는 것
- vRAM: VM에게 할당된 가상 메모리

#### Virtual Disk
- VM에는 CPU와 RAM 뿐만 아니라 디스크도 필요함
- VM의 입장에서는 자신의 하드디스크임
- 물리 서버의 입장에서는 물리 서버의 저장장치 어딘가에 저장된 데이터임

#### 클라우드
클라우드 업체가 거대한 물리 서버들을 가지고 있음
사용자는 물리 서버를 직접 사거나 만드는 대신 "VM 하나 만들어주세요"라고 요청하는 것임
업체는 자원을 나누어 주고 금전적인 대가를 받음

### Storage
서버에서 데이터를 어디에 저장할 것인가?

#### HDD와 SSD
- 물리적인 저장장치

|HDD|SSD|
|디스크가 물리적으로 회전하면서 데이터를 저장|반도체를 이용해 데이터를 저장|
|저렴함|HDD보다 비쌈|
|대용량에 유리|소음이 적음|
|SSD보다 느림|빠름|

#### Block Storage
VM에 디스크를 하나 붙이는 것

#### File Storage
파일 단위로 저장
- 여러 서버가 파일을 공유해야 할 때 사용하는 저장 방식
```
/shared
 ├── report.pdf
 ├── image.jpg
 ├── video.mp4
 └── document.txt
```

여러 서버가 하나의 파일 저장공간을 같이 사용할 수 있음
```
VM1 ──┐
VM2 ──┼── File Storage
VM3 ──┘
```


#### Object Storage
대표적으로 이미지, 영상, 백업 파일처럼 파일 형태의 데이터를 대량으로 저장하는 데 사용

#### Block vs File vs Object
| 종류                 | 쉽게 생각하면     | 주요 용도         |
| ------------------ | ----------- | ------------- |
| **Block Storage**  | 서버에 붙이는 디스크 | VM 디스크, DB    |
| **File Storage**   | 공유 폴더       | 여러 서버의 파일 공유  |
| **Object Storage** | 파일 보관 창고    | 이미지, 영상, 백업 등 |

Block = 디스크
File = 공유 폴더
Object = 파일 저장 창고

---

#### Volume
저장공간의 논리적인 단위

#### Snapshot
특정 시점의 디스크 상태를 찍어놓는 것

예를 들어 정상적으로 작동하고 있는 서버를 [10:00]상태 저장을 해놓는 것
-> 11:00에 문제가 발생하면 Snapshot을 이용해서 이전 상태로 복구

- **Backup** vs **Snapshot**
    - Backup: "데이터를 별도로 안전하게 보관하자"
    - Snapshot: "이 순간의 상태를 찍어놓자"


### 정리
1. Hypervisor = 물리 서버를 여러 VM으로 나눠 관리
2. VM = 가상으로 만든 서버
3. vCPU / vRAM = VM에게 할당한 CPU / RAM
4. Block / File / Object = 대표적인 스토리지 방식
5. Cloud = Computing(계산하고 실행하는 것) + Network + Storage

```
             Cloud Infrastructure
                    │
       ┌────────────┼────────────┐
       ↓            ↓            ↓
   Computing     Network      Storage
       │            │            │
      VM          IP/Port      Disk
     vCPU       Load Balancer   File
     vRAM        Firewall      Object
```