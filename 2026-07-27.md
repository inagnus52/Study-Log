# 2026-07-27 월요일
## 오늘 배울 내용
- root와 일반 사용자
- 사용자와 그룹
- 파일 소유자와 소유 그룹
- 읽기·쓰기·실행 권한
- 최소 권한 원칙

```
id
who
sudo
useradd
adduser
passwd
groupadd
usermod
groups
```

```
chmod
chown
chgrp
ls-l
```

- 숫자 권한 `755`, `644`, `700`
- `rwx` 권한 해석

- `admin`
- `developer`
- `viewer`

각 계정에 서로 다른 파일 권한을 설정합니다.
다음 권한을 해석해 봅니다.
```
-rwxr-x
-rw-r--r--
drwx------
```


## 오늘 배운 내용
#### root 사용자와 일반 사용자
- root 사용자: `root`는 리눅스에서 거의 모든 작업을 할 수 있는 최고 관리자 계정
    - 사용자 생성 및 삭제
    - 비밀번호 변경
    - 파일 소유자 변경
    - 프로그램 설치
    - 시스템 설정 변경
    - 다른 사용자의 파일 접근
    -root 권한은 매우 강력하여, 평소에는 일반 사용자로 작업하고, 관리자 권한이 필요할 때만 `sudo`를 사용한다
    ```sudo 명령어```

        예시) 프로그램 목록 갱신
```sudo apt update```

<br>

- 일반 사용자: 자신에게 허용된 파일과 명령만 사용 가능

    |명령어|설명|
    |---|---|
    |whoami|현재 로그인한 사용자 확인|
    |id|현재 사용자의 UID, 그룹 등의 자세한 정보 확인|

**실습**
![사용자 계정 실습](/images/사용자%20계정%20실습.png)
- uid=1000(parkseonga): 일반 사용자 계정이라는 뜻 <br>
    - `0 → root`-> 사용자
    - `1~999` → 시스템에서 사용하는 계정
    - `1000 이상` → 일반 사용자 계정
- gid=1000(parkseonga): 기본 그룹의 GID가 `1000`이고 기본 그룹의 이름이 `parkseonga`라는 뜻
- groups=1000(parkseonga),4(adm),24(cdrom),27(sudo),30(dip),46(plugdev),100(users): `parksenga` 사용자는 여러 그룹에 속해 있음
    | 그룹           | 의미                    |
    | ------------ | --------------------- |
    | `parkseonga` | 사용자의 기본 그룹            |
    | `adm`        | 일부 시스템 로그를 읽을 수 있는 그룹 |
    | `cdrom`      | CD/DVD 장치 사용 권한       |
    | `sudo`       | 관리자 권한 명령 실행 가능       |
    | `dip`        | 네트워크 연결 관련 권한         |
    | `plugdev`    | USB 등 외부 장치 사용 권한     |
    | `users`      | 일반 사용자 그룹             |

- 27(sudo): 가장 중요한 부분으로 현재 계정은 관리자 권한을 사용할 수 있다는 뜻
```sudo 명령어```

<br>

- root와 일반 사용자의 차이를 확인해 볼 수 있는 방법
    ```
    whoami
    sudo whoami
    ```
    예상 결과
    ```
    parkseonga
    root
    ```

#### 사용자와 그룹을 직접 생성하기
`developer` 계정 생성할 것임
```
sudo adduser developer
```

실행하면 다음 정보를 차례로 물어봅니다.
```New password:
Retype new password:
Full Name []:
Room Number []:
Work Phone []:
Home Phone []:
Other []:
Is the information correct? [Y/n]
```
계정 생성이 끝나면 아래 명령어로 확인
```
id developer
```
예상 결과
```
uid=1001(developer) gid=1001(developer) groups=1001(developer), 100(users)
```
- `developer` 사용자의 UID는 `1001`이다
- 기본 그룹도 `developer`이고, GID는 `1001`이다
- `developer` 사용자는 `developer`이라는 기본 그룹과, `users`라는 일반 사용자 그룹 이렇게 두 그룹에 속해 있다.


- 원래 계정으로 돌아가고 싶으면 `exit`을 입력하면 된다.
- 그 다음 `whoami` 로 확인해서 `parkseonga`가 나오면 정상
![그룹 생성 실습](/images/그룹%20생성%20실습.png)


**명령어 정리**
|명령어|설명|결과|
|---|---|---|
|sudo groupadd devteam|devteam이라는 그룹 생성| - |
|getent group devteam|시스템에 devteam 그룹이 있는지 확인|devteam:x:1002:|
|sudo usermod -aG devteam developer|developer 사용자를 devteam 그룹에 추가| - |
|groups developer|developer가 속한 그룹을 확인| - |
- `sudo usermod -aG devteam developer`
    - `usermod`: 사용자 정보 수정
    - `-G`: 보조 그룹 지정
    - `-a`: 기존 그룹을 유지하면서 추가
    - `devteam`: 추가할 그룹
    - `developer`: 대상 사용자
    - `-a`를 빼면 기존 보조 그룹이 사라질 수 있어서 보통 `-aG`를 같이 사용함
- `groups developer`
    - `developer`: 기본 그룹
    - `users`: 기존 보조 그룹
    - `devteam`: 새로 추가한 그룹

#### 파일 소유자와 소유 그룹 확인 실습
**실습용 파일 만들기**

![파일 소유자와 소유 그룹 확인 실습](/images/파일%20소유자와%20소유%20그룹%20확인%20실습.png)
```
parkseonga parkseonga
```
- 첫 번째 `parkseonga`: 파일 소유자
- 두 번째 `parkseonga`: 파일 소유 그룹

**파일 소유 그룹 변경**

![파일 소유 그룹 변경 실습](/images/파일%20소유%20그룹%20변경%20실습.png)
- 첫 번째 `parkseonga` → 파일 소유자
- 두 번째 `devteam` → 파일 소유 그룹
- `chgrp` → **change group**, 파일의 소유 그룹 변경

**파일 소유자 변경**

![파일 소유자 변경](/images/파일%20소유자%20변경.png)
- `developer`: 파일 소유자
- `devteam`: 파일 소유 그룹
- `chown`: **change owner**, 파일 소유자 변경

#### 권한 변경 실습
- `chmod`: 권한을 직접 바꾸는 명령어

![권한 변경 실습](/images/권한%20변경%20실습.png)

|권한|권한자|의미|
|---|---|---|
|6|소유자|읽기+쓰기|
|4|그룹|읽기|
|0|그 외 사용자|권한 없음|

**맨 앞의 `-`는 일반 파일이라는 뜻임**
```
rw-|r--|---
```

![권한 변경 실습2](/images/권한%20변경%20실습2.png)
**rwx의 의미**
```
rmx|---|---
```

4+2+1=7
|권한|권한자|의미|
|---|---|---|
|4|소유자|r|
|2|소유자|w|
|1|소유자|x(excute=실행)|
->따라서 `chmod`는 소유자에게만 `rwx` 권한을 주는 것임

![권한 변경 실습3](/images/권한%20변경%20실습3.png)
```
rwx|r-x|r-x
```

- 첫 번째 `-` : 일반 파일
- 첫 번째 `rwx` : 소유자는 읽기, 쓰기, 실행 모두 가능
- 두 번째 `r-x` : 소유 그룹은 읽기와 실행만 가능
- 세 번째 `r-x` : 그 외 사용자는 읽기와 실행만 가능

#### 종합 권한 실습(adminuser, developer, viewer 계정)
```
#계정이 있는지 확인
id admin
id developer
id viewer

#없는 계정 생성
sudo adduser adminuser
sudo adduser viewer

#실습용 그룹 만들기
sudo groupadd projectteam

#그룹에 계정 추가하기
sudo usemod -aG projectteam adminuser
sudo usemod -aG projectteam developer
sudo usemod -aG projectteam viewer

#확인
groups adminuser
groups developer
groups viewer

#공유 폴더를 만들고 그룹 권한 설정하기
sudo mkdir /project
sudo chown adminuser:projectteam /project
sudo chmod 770 /project
ls -ld /project
```

결과는 이렇게 나옴
```
drwxrwx--- 2 adminuser projectteam ... /project
```
- 맨 앞의 d는 directory 라는 뜻
- |rwx|rwx|---|
    - 소유자(adminuser): 읽기+쓰기+접근 가능
    - 그룹(projectteam): 읽기+쓰기+접근 가능
    - 그 외 사용자: 접근 불가