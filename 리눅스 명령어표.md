|**명령어**|**설명**|
|---|---|
|mkdir|디렉터리 만들기|
|cd|change directory의 약자로, 현재 작업 중인 디렉터리를 이동|
|cd ~|홈 디렉터리로 이동|
|cd ..|상위 폴더로 이동|
|cd /|최상위 디렉터리로 이동|
|cd -|바로 전에 있던 디렉터리로 이동|
|pwd|현재 위치 확인|
|touch|새 빈 파일을 만들거나, 기존 파일의 수정 시간을 갱신|
|echo|문자열을 화면에 출력하고 >,>>과 함께 사용하면 내용 저장|
|ls|list의 약자로 현재 디렉터리 안에 있는 파일과 폴더 목록을 보여주는 명령어|
|ls -l|권한, 크기, 수정 시간까지 자세히 보기|
|ls -a|숨김 파일까지 모두 보기|
|ls -al|숨김 파일을 포함해 자세히 보기|
|ls -R|하위 폴더 내용까지 전부 보기|
|ls -ld|특정 디렉터리 자체의 권한과 소유자 정보를 확인하기 위해 사용|
|mv|파일이나 폴더를 이동합니다|
|rm|파일을 삭제|
|rmdir|빈 디렉터리만 삭제|
|cp|파일이나 폴더를 복사|
|grep|파일에서 특정 단어나 문장이 있는 줄을 찾음|
|id|현재 접속한 사용자나 특정 계정의 사용자 ID(UID), 기본 그룹 ID(GID), 그리고 소속된 모든 보조 그룹 정보를 확인|
|id -u|실제 사용자 ID(UID) 숫자만 출력|
|id -g|기본 그룹 ID(GID) 숫자만 출력|
|id -G|소속된 모든 그룹의 ID를 출력|
|id -n|ID 숫자 대신 그룹 이름이나 사용자 이름으로 출력|
|getent|사용자, 그룹, 호스트 정보 확인|
|(sudo) adduser|계정 생성|
|(sudo) groupadd|그룹 생성|
|usermod -aG|그룹에 계정 추가|
|groupds|해당 계정의 그룹 확인|
|chgrp|change group, 파일의 소유 그룹 변경|
|chown|change owner, 파일 소유자 변경|
|chmod|권한을 직접 바꾸는 명령어|
|gpasswd|그룹 구성원 관리|
|gpasswd -d|delete, 그룹에서 사용자 제거|
|gpasswd -a|add, 그룹에 사용자 추가|
|su|계정 전환|
|ps|프로세스 출력|
|ps aux|프로세스 전체 목록 출력|
|\||왼쪽 명력어의 출력 결과를 오른쪽 명령어로 전달
|---|------|
|sleep| |
|jobs|현재 실행 중인 프로세스 보기|
|fg %-|백그라운드에서 실행 중인 프로세스를 앞으로 가져오기|
|bg %-|프로세스를 백그라운드로 보내기|
|ctrl+c|프로세스 종료|
|ctrl+z|프로세스 일시 종료|
|kill|프로세스 종료|
|killall|해당 이름의 프로세스 전부 종료|
|top|현재 실행 중인 프로세스들을 실시간으로 보는 명령어|