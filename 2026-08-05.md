# 2026-08-05 수요일
## 오늘 배울 내용
**원격접속과 리눅스 네트워크 명령어**

- SSH란 무엇인가
- 서버 IP 주소
- 포트 번호
- 로컬 접속과 원격 접속
- 공개키와 개인키 개념

```
ip addr
ip route
hostname
hostname-I
ping
ss-tulpncurlwget
```



## 오늘 배운 내용
### SSH
- **SSH**: Secure Shell, 네트워크를 통해 다른 컴퓨터의 터미널을 원격으로 안전하게 사용하는 기술

```
ssh parkseonga@172.20.10.5
```

- `172.20.10.5` → 접속할 컴퓨터
- `parkseonga` → 그 컴퓨터에서 사용할 사용자 계정
- `ssh` → 원격 접속 방식

### IP 주소
```
id addr
```
- Ubuntu에 연결된 네트워크 장치와 IP 주소를 모두 확인하는 명령어

![Ubuntu에 연결된 ip 주소 찾기 실습](/images/Ubuntu에%20연결된%20ip%20찾기%20실습.png)
- Ubuntu의 현재 IP 주소: `172.30.246.174`
- eth0 부분의 이 줄: `inet 172.30.246.174/20`
    각 네트워크 장치
    - `lo`: 자기 자신과 통신하는 가상 장치(loopback의 약자), 실제로 존재하는 랜카드가 아니라 Ububtu가 자기 자신과 통신하기 위해 만든 가상 네트워크 장치
    - `127.0.0.1`: 언제나 현재 명령을 실행하고 있는 자기 자신을 가리키는 주소(localhost)
    - `eth0`: Ubuntu가 외부 네트워크와 통신할 때 사용하는 장치
        - 예를 들어 Ubuntu에서 `curl https://example.com`을 실행하면 요청은 `Ubuntu -> eth0 -> Windows -> 공유기 -> 인터넷 서버` 경로로 이동한다
        -`127.0.0.1`로 요청하면 자기 자신을 뜻하므로 요청이 밖으로 나가지 않는다
        ```
        Ububtu의 프로그램 -> lo(루프백) -> Ubuntu의 다른 프로그램
        ```
    - `172.30.246.174`: Ubuntu의 현재 IPv4 주소, `eht0`에 할당된 Ubuntu의 IPv4 주소
        - `127.0.0.1`과 `172.30.246.174`는 둘 다 Ubuntu 자신을 가리킬 수 있지만, 사용 목적과 통신 범위가 다름

            | 주소               | 의미                     | 통신 범위                          |
            | --- | --- | --- |
            | `127.0.0.1`      | 지금 명령을 실행하는 자기 자신      | Ubuntu 내부 전용                   |
            | `172.30.246.174` | Ubuntu의 `eth0`에 할당된 주소 | Windows 등 외부에서 Ubuntu를 찾을 때 사용 |

            +)
            Windows에서:
            ```
            ssh parkseonga@127.0.0.1
            ```
            이라고 하면 `127.0.0.1`은 기본적으로 Windows 자신을 뜻ㅎㅁ
            반면,
            ```
            ssh parkseonga@172.30.246.174
            ```
            라고 하면 `172.30.246.174`라는 주소를 가진 WSL Ubuntu를 찾아감.

            즉:

            127.0.0.1은 누구든 “나 자신”이라고 부르는 주소이고,
            172.30.246.174는 네트워크에서 Ubuntu를 구별하기 위해 Ubuntu에 할당된 주소입니다.

            비유하면 127.0.0.1은 “나”, 172.30.246.174는 “내 주소”에 가깝습니다.


    - `/20`: IP 주소 중 어디까지가 네트워크 범위인지 나타내는 값
        - `/20`은 IP 주소 32비트 중 앞의 20비트가 네트워크를 구분하는 부분이라는 뜻임

        현재 설정을 실제 범위로 계산하면 다음과 같음

        | 구분        | 주소               |
        | --------- | ---------------- |
        | 네트워크 주소   | `172.30.240.0`   |
        | Ubuntu 주소 | `172.30.246.174` |
        | 브로드캐스트 주소 | `172.30.255.255` |

### 포트 번호
- 포트 번호: 한 컴퓨터 안에서 어떤 프로그램(서비스)으로 연결할지 구분하는 번호
    - IP 주소 → 어느 컴퓨터로 갈지
    - 포트 번호 → 그 컴퓨터의 어떤 서비스로 갈지

    <br>

    - 예를 들어 Ubuntu의 IP가 `172.30.246.174`이고 SSH 명령 기본 포트는 `22`이므로
    ```
    172.30.246.174:22
    └── 컴퓨터 주소 └ SSH 서비스
    ```
    
    -하나의 Ubuntu에서는 여러 포트가 동시에 존재할 수 있음
    ```
    172.30.246.174:22   → SSH 원격 접속
    172.30.246.174:80   → HTTP 웹 서비스
    172.30.246.174:443  → HTTPS 웹 서비스
    ```
    다만 포트 번호가 있다고 해서 서비스가 자동으로 실행되는 것은 아닙니다. 해당 포트에서 서버 프로그램이 실제로 기다리고 있어야 접속할 수 있습니다.


### 로컬 접속과 원격 접속의 차이
- 로컬 접속: 현재 사용 중인 컴퓨터에 직접 로그인해서 사용하는 것
    ex) Ubuntu 터미널을 직접 열어 명령어를 실행하면 로컬 접속
        ```
        사용자 → Ubuntu 터미널 → Ubuntu
        ```

- 원격 접속: 다른 컴퓨터에서 네트워크를 통해 서버에 접속하는 것
    ex) Windows PowerShell에서 WSL Ubuntu로 SSH 접속하면 원격 접속 방식
    ```
    ssh parkseonga@172.30.246.174
    ```

### 공개키와 개인키
SSH 키는 비밀번호 대신 본인임을 증명하는 한 쌍의 파일입니다.

개인키 → 내가 비밀로 보관하는 열쇠
공개키 → 접속할 서버에 등록하는 열쇠 확인용 정보

접속 준비는 다음과 같습니다.

```
Windows                         Ubuntu 서버
개인키 보관                     공개키 등록
절대 공유하지 않음              다른 곳에 전달해도 됨
```
SSH로 접속하면 Ubuntu는 등록된 공개키를 이용해 확인합니다.

“접속하려는 사람이 이 공개키와 짝이 되는 개인키를 정말 가지고 있는가?”

Windows가 개인키를 직접 보내지는 않고, 개인키를 가지고 있다는 사실만 증명합니다. 증명에 성공하면 로그인을 허용합니다.

비밀번호 방식과 비교하면:

방식	서버에 미리 저장하는 것	접속할 때 사용하는 것
비밀번호 접속	비밀번호 검증 정보	비밀번호 입력
SSH 키 접속	공개키	내 컴퓨터의 개인키

가장 중요한 규칙은 이것입니다.

공개키는 서버에 등록하고, 개인키는 절대로 다른 사람에게 보내거나 서버에 함부로 올리지 않습니다.

나중에 ssh-keygen으로 키를 만들면 보통 다음 파일이 생성됩니다.

id_ed25519      # 개인키
id_ed25519.pub  # 공개키

.pub이 붙은 파일이 공개키입니다.

### 네트워크 명령어 실습
#### `ip route`
```
ip route
```
- Ubuntu가 목적지에 따라 어느 길로 데이터를 보낼지 보여줌

![ip route 명령어 실습](/images/ip%20route%20명령어%20실습.png)

- 첫 번째 줄:
    ```
    default via 172.30.240.1 dev eth0
    ```
    특별히 정해진 경로가 없는 목적지, 즉 주로 인터넷이나 다른 네트워크로 갈 때 `eth0` 장치를 통해 `172.30.240.1`로 먼저 보내라
    - default → 별도의 경로가 없을 때 사용하는 기본 경로
    - via 172.30.240.1 → 데이터를 먼저 전달할 곳인 기본 게이트웨이
    - dev eth0 → 데이터를 내보낼 네트워크 장치

    WSL 환경에서 172.30.240.1은 Ubuntu가 바깥으로 나갈 때 통과하는 Windows 쪽 관문이라고 이해하시면 됩니다.

    `Ubuntu → eth0 → 172.30.240.1(관문) → Windows → 외부 네트워크`


- 두 번째 줄:
    ```
    172.30.240.0/20 dev eth0 proto kernel scope link src 172.30.246.174
    ```
    목적지가 172.30.240.0/20 네트워크 안에 있다면, 별도의 관문을 거치지 않고 eth0를 통해 직접 통신하라는 뜻입니다.
    

    - `172.30.240.0/20` → 현재 Ubuntu가 속한 네트워크 범위
    - `dev eth0` → eth0 장치 사용
    - `scope link` → 같은 네트워크에 직접 연결되어 있음
    - `src 172.30.246.174` → 통신할 때 Ubuntu가 출발지 주소로 사용할 IP
    - `proto kernel` → Ubuntu 커널이 네트워크 설정을 보고 자동으로 만든 경로


#### `hostname`
```
hostname
```
- 현재 컴퓨터에 설정된 이름을 확인하는 명령어

![hostname 명령어 실습](/images/hostname%20명령어%20실습.png)

    현재 터미널에 보이는
    ```
    parkseonga@DESKTOP-4CLG47Q
    ```
    에서:
    - `parkseonga` → 로그인한 사용자명
    - `DESKTOP-4CLG47Q` → 컴퓨터의 호스트 이름
- 즉 `hostname`을 실행하면 `DESKTOP-4CLG47Q`가 나올 것임

```
hostname -I
```
- IP 주소만 간단하게 확인하는 명령어

    ```
    172.30.246.174
    ```
    앞서 ip addr에서 확인한 eth0의 주소와 같습니다.

    - `ip addr` → 네트워크 장치와 IP를 자세히 확인
    - `hostname -I` → IP 주소만 빠르게 확인
    -- 대문자 I는 IP address를 의미

    현재 SSH 접속에 사용할 Ubuntu 주소는 `172.30.246.174`입니다.

#### 네트워크 연결 확인 명령어 실습
```
ping -c 4 172.30.240.1
```
- `-c 4`는 ping 요청을 네 번 보낸 뒤 자동으로 종료하라는 뜻임

![ping -c 4 명령어 실습](/images/ping%20-c%204%20명령어%20실습.png)
- Ubuntu가 `172.30.240.1`에 신호를 4번 보냄
- 상대가 4번 모두 응답함
- 손실률 `0%` → 연결 상태 정상
- 평균 응답 시간 `2.263ms` → 약 0.002초로 매우 빠름
- `172.30.240.1` → WSL Ubuntu가 외부로 나갈 때 거치는 Windows 쪽 게이트웨이

- `icmp_seq=1` → 첫 번째 요청
- `ttl=128` → 패킷이 이동할 수 있는 남은 횟수
- `time=2.82 ms` → 요청과 응답에 걸린 시간

#### 사용 중인 포트 확인 명령어 실습
```
sudo ss -tulpn
```
- 현재 사용 중인 포트를 확인하는 명령어
    옵션의 의미:
    - `-t` → TCP 확인
    - `-u` → UDP 확인
    - `-l` → 접속을 기다리는 포트만 확인
    - `-p` → 포트를 사용하는 프로그램 확인
    - `-n` → 서비스 이름 대신 실제 포트 번호로 표시

![포트 확인 명령어 실습](/images/포트%20확인%20명령어%20실습.png)

- 53번 → DNS 서비스가 사용하는 대표 포트
- 323번 → chronyd가 내부 통신에 사용하는 포트
- 127.0.0.1, 127.0.0.53, 127.0.0.54 → Ubuntu 내부에서만 접근 가능한 주소
- 0.0.0.0:* → 특정 상대가 정해져 있지 않음
- [::]와 [::1] → IPv6 방식의 주소

상태의 의미:

- LISTEN → TCP 프로그램이 접속 요청을 기다리는 상태
- UNCONN → 연결을 미리 맺지 않는 UDP 소켓의 일반적인 대기 상태

#### 웹 서버에 요청 보내기 명령어 실습
```
curl https://example.com
```
- 웹 서버에 요청을 보내 응답을 확인
![curl 명령어 실습](/images/curl%20명령어%20실습.png)

**HTTP 응답 헤더 확인**
```
curl -I https://example.com
```
- 본문 전체가 아니라 응답 정보만 확인
- 대문자 `I`는 HTML 본문 대신 HTTP 응답 헤더만 확인하는 옵션
![curl -I 명령어 실습](/images/curl%20-I%20명령어%20실습.png)

#### DNS 확인 실습**

- DNS: Domain Name System

```
example.com → DNS 조회 → IP 주소
```
- **실제 진행 과정**
    - Ubuntu에서 `curl https://example.com` 명령어 실행
    - 내부에서는 대략 다음 순서로 처리
        ```
        1. curl이 example.com에 접속하려고 함
        2. Ubuntu가 example.com의 IP 주소를 DNS에 질문함
        3. DNS가 IP 주소를 알려줌
        4. Ubuntu가 받은 IP 주소의 서버로 접속함
        5. 서버에 HTTPS 요청을 보냄
        6. 서버가 HTML 등의 응답을 돌려줌
        ```
    - 중요한 점은 DNS는 웹페이지를 보내주는 서비스가 아니라, 서버의 IP 주소를 알려주는 서비스

**DNS 확인 실습**

```
getent hosts example.com
```
- `getent hosts` 명령어는 도메인 이름인 `example.com` 이 어떤 IP 주소로 변환되는지 확인함

![DNS 확인 실습](/images/DNS%20조회%20명령어%20실습.png)
- 도메인이 두 개의 IP 주소로 조회되었다.
- 하나의 도메인이 여러 서버 주소를 가질 수 있으며, 이를 통해 접속 요청을 분산하거나 장애에 대비할 수 있다.
- 사용자와 가까운 서버로 연결하여 속도가 향상된다는 장점도 있음

**IPv4와 IPv6**
- 둘 다 IP 주소 체계임
- 둘의 큰 차이는 만들 수 있는 주소의 개수
| 구분    | IPv4             | IPv6                      |
| ----- | ---------------- | ------------------------- |
| 주소 예시 | `172.30.246.174` | `2606:4700:10::6814:179a` |
| 구분 기호 | 점(`.`)           | 콜론(`:`)                   |
| 숫자 표현 | 10진수             | 16진수                      |
| 주소 길이 | 32비트             | 128비트                     |
| 주소 개수 | 약 43억 개          | 사실상 매우 많음                 |
| 현재 사용 | 여전히 가장 널리 사용     | IPv4 부족 문제를 해결하기 위해 확대 중  |

**IPv4의 주소만 조회하는 명령어**
```
getent ahostsv4
```
- 도메인에 연결된 IPv4 주소만 조회하는 명령어
- 실행 결과에는 두 개의 주소가 나타남
    - `104.20.23.154`
    - `172.66.147.243`
- 같은 IP가 세 번 출력된 것은 서버가 여섯 대라는 뜻이 아니고 각 주소를 사용할 수 있는 통신 방식이 함께 표시된 것
표시	 의미	                            주로 사용하는 프로토콜
STREAM	연결을 맺고 데이터를 연속적으로 전송    TCP
DGRAM	데이터를 독립된 단위로 전송	            UDP
RAW	    가공되지 않은 IP 패킷을 직접 처리	    원시 소켓

#### 목적지 서버까지 패킷이 어떤 경로를 거치는지 확인하는 명령어 실습
```
tracepath example.com
```

NOT FOUND 나오면 iputils-tracepath 패키지 설치(`sudo apt install iputils-tracepath`)