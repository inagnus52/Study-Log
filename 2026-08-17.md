# 2026-08-17 월요일
## 오늘 배울 내용
**Cloud 기본 개념**
- Cloud Computing
- On-premise
- Public Cloud
- Private Cloud
- Hybrid Cloud
- IaaS
- PaaS
- SaaS

**Cloud Architecture**
- Compute
- Network
- Storage
- Security
- Load Balancer
- Monitoring

## 오늘 배운 내용
### VPC와 Subnet
- VPC: 클라우드 안에서 내가 직접 구성하는 하나의 네트워크 공간
```
Cloud
└── VPC
    ├── Web Server
    ├── WAS
    └── DB
```

- Subnet: 하나의 네트워크를 여러 개의 작은 네트워크로 나눈 것
```
VPC
│
├── Public Subnet
│   └── 192.168.1.0/24
│
└── Private Subnet
    └── 192.168.2.0/24
```
    - 왜 Subnet을 나누는가
        : Web Server와 DB를 똑같은 네트워크에 넣을 필요가 없음
        사용자가 직접 접근해야 하는 것은 보통 Web Server


### Public / Private
- Public: 인터넷과 직접 통신할 수 있도록 네트워크 경로가 구성되어 있느냐
(인터넷에 무조건 공개된다는 뜻 X)

```
VPC
│
├── Public Subnet
│   └── 192.168.1.0/24
│
└── Private Subnet
    └── 192.168.2.0/24
```

이건 Private
```
Internet
   X
Private Subnet
   ↓
WAS
```
직접 인터넷과 통신할 수 없음

### CIDR
```
192.168.1.0/24
```
-> 이게 CIDR 표기
-> `24`는 앞의 24비트를 네트워크 영역으로 사용한다는 의미


### Routing / NAT / Firewall
1. **Routing**
: 네트워크 내에서 또는 여러 네트워크 간에 트래픽 경로를 선택하는 과정
"이 패킷을 어떤 경로로, 어디로 보내야 할까?" 라는 뜻

- Route Table
: 목적지에 따라 패킷을 어디로 보낼지 적어놓은 표
예시)
```
Destination        Target

192.168.1.0/24     Local
0.0.0.0/0          Internet Gateway
```

- Gateway
: 다른 네트워크로 나가는 통로


2. **NAT**(Network Address Translation)
: Private 서버가 자신의 사설 IP를 직접 공개하지 않고 인터넷으로 나갈 수 있게 해주는 역할
-사설 네트워크(Private Network)에 속한 여러 개의 호스트가 하나의 공인 IP 주소를 사용하여 인터넷에 접속하고 싶을 때 NAT를 사용한다


3. **Firewall**
경로가 있다고 해서 모든 통신을 허용하게 되면 위험하다.
80번 포트는 허용하고, DB의 3306 같은 포트는 차단할 수 있다.
이런 식으로 통신을 제어하는 것이 Firewall의 역할임.

- **Security Group**
: 대표적인 클라우드 방화벽 개념

예를 들어서 웹 서버에 
```
Inbound

80  → 허용
443 → 허용
22  → 특정 IP만 허용
```
처럼 허용할 수 있음

- **ACL(Access Control List)**
네트워크 접근 제어
```
Security Group
→ 서버/네트워크 인터페이스 수준에서 접근 제어

ACL
→ Subnet 수준에서 접근 제어
```


### 최종 정리 그림
```
                         Internet
                            │
                            ↓
                  ┌─────────────────┐
                  │ Internet Gateway│
                  └────────┬────────┘
                           │
                    ┌──────┴──────┐
                    │     VPC     │
                    │             │
                    │ Public      │
                    │ Subnet      │
                    │             │
                    │ Web Server   │
                    │      │      │
                    │     NAT     │
                    │      │      │
                    │ Private     │
                    │ Subnet      │
                    │             │
                    │ WAS         │
                    │  │          │
                    │ DB          │
                    └─────────────┘
```

- 패킷 이동 그림
```
IP 주소
  ↓
Route Table
  ↓
Gateway / NAT
  ↓
Firewall / Security Group / ACL
  ↓
목적지
```