# 2026-08-11 화요일
## 오늘 배울 내용
**네트워크**
- 네트워크
- LAN / WAN
- 인터넷
- 패킷
- MAC 주소
- IP 주소
- IPv4
- IPv6
- 공인 IP
- 사설 IP
**IP와 Subnet**
- IP 주소 구조
- 네트워크 주소
- 호스트 주소
- Subnet Mask
- CIDR
- `/24`
- `/16`
- `/8`

- 라우터
- 게이트웨이
- 실제 통신 흐름


## 오늘 배운 내용
**"내 컴퓨터에서 서버까지 데이터가 어떻게 가는가?"**를 중심으로

### 네트워크
- 네트워크: 컴퓨터끼리 데이터를 주고받을 수 있도록 연결된 것

- LAN(Local Area Network): 가까운 범위의 네트워크
    - 집
    - 학교
    - 회사
    - 데이터센터
- WAN(Wide Area Network): 멀리 떨어진 네트워크

#### 인터넷
- 인터넷 = 전 세계의 수많은 네트워크가 서로 연결된 거대한 네트워크
- Inter + Network
```
내 컴퓨터
   │
   ▼
우리 집 네트워크
   │
   ▼
인터넷
   │
   ▼
네이버 서버
```

#### 패킷(Packet)
- 패킷: 컴퓨터가 서버로 큰 데이터를 보낼 때 데이터를 통째로 보내는 것이 아니라 작은 조각으로 나눠서 보내는데, 이 작은 조각을 패킷이라고 함.
    - 컴퓨터가 데이터를 보낼 때 쪼개고, 서버에서 다시 합쳐서 보게 됨

#### 컴퓨터 주소
- 집 주소가 있어야 택배가 올 수 있듯이 컴퓨터에서 주소가 있음!!
- 대표적인 두 가지 주소:
    - **MAC 주소**: 네트워크 장치 자체를 식별하는 주소
        - MAC 주소는 보통 이렇게 생겼다 (48비트=6바이트)
            ```
            A4:5E:60:12:34:56
            ```
            또는
            ```
            A4-5E-60-12-34-56
            ```
        - 컴퓨터 자체보다는 네트워크 인터페이스(NIC)를 식별한다(유선 랜카드, WI-Fi 랜카드, 네트워크 카드 등)
    - **IP 주소**: 네트워크에서 목적지를 찾기 위한 주소
        - IPv4: 32비트 주소
        - IPv6: 128비트 주소
        - 하나의 IP 주소 안에는 크게 두 가지 정보가 들어감
            ```
            192.168.1 | 10
            ─────────   ──
            네트워크     호스트
            부분        부분
            ```
            - **네트워크 부분** -> 어느 네트워크에 속해 있는가?
            - **호스트 부분** -> 그 네트워크 안에서 어떤 장치인가?

#### 공인 IP와 사설 IP
- **사설 IP**: 내부 네트워크에서 사용하는 IP
```
10.0.0.0 ~ 10.255.255.255
172.16.0.0 ~ 172.31.255.255
192.168.0.0 ~ 192.168.255.255
```

- **공인 IP**: 인터넷에서 사용되는 IP
 ```
 내 PC
192.168.1.10
   │
   ▼
공유기
공인 IP
   │
   ▼
인터넷
```

#### 서브넷 마스크
- 서브넷 마스크: IP 주소에서 네트워크 부분과 호스트 부분을 구분해주는 값
예를 들어:
```
192.168.1.10/24
```
- `192.168.1.10` -> IP 주소
- `/24` -> 서브넷 마스크를 **CIDR 방식**으로 표현한 것
    "앞 24비트가 네트워크구나." 라고 이해하면 됨
    **"192.168.1은 네트워크, 10은 호스트구나."**

| CIDR  | 서브넷 마스크         | 네트워크 부분 | 호스트 부분 |
| ----- | --------------- | ------- | ------ |
| `/8`  | `255.0.0.0`     | 8비트     | 24비트   |
| `/16` | `255.255.0.0`   | 16비트    | 16비트   |
| `/24` | `255.255.255.0` | 24비트    | 8비트    |


#### 네트워크 주소
```
192.168.1.10/24
```
/24니까:
```
192.168.1 | 10
─────────   ──
네트워크    호스트
```
여기서 호스트 부분을 전부 0으로 만든 주소가 네트워크 주소입니다.
```
192.168.1.0
```
-> `192.168.1.10/24`의 네트워크 주소는 `192.168.1.0`

---
```
192.168.1.20/24
```
똑같이 /24이므로:
```
192.168.1 | 20
```
호스트 부분을 0으로 만들면:
```
192.168.1.0
```
즉,
```
192.168.1.10/24
192.168.1.20/24
192.168.1.30/24
```
이 세 장치는 모두:
```
네트워크 주소
192.168.1.0
```
이라는 같은 네트워크에 있는 것임!!

---
**네트워크의 특징**
- 네트워크 주소는 장치에 할당하는 주소가 아님!
- 네트워크 자체를 나타내는 것임!!

```
192.168.1.0   ← 네트워크 주소
192.168.1.1   ← 장치
192.168.1.2   ← 장치
...
192.168.1.254 ← 장치
192.168.1.255 ← 브로드캐스트
```

#### 브로드캐스트 주소
- 브로드캐스트 주소: 해당 네트워크의 모든 장치에게 데이터를 전달할 때 사용하는 주소

호스트 부분을 전부 1로 만들면:
```
11111111
```
이걸 10진수로 바꾸면 255입니다.

그래서:
```
192.168.1.255
```
가 됩니다.

이것을 브로드캐스트 주소라고 합니다.

---
***중요~~***
```
호스트 비트를 전부 0 → 네트워크 주소
호스트 비트를 전부 1 → 브로드캐스트 주소
```


---
***실제로 해보시오***
**`10.0.0.10/24`**
1. 네트워크 주소
: 호스트 부분을 0으로 만듦
```
10.0.0.0
```

2. 브로드캐스트 주소
: 호스트 부분을 전부 1로 만듦
```
10.0.0.255
```

3. 사용할 수 있는 IP
: 양 끝을 제외하면
```
10.0.0.1 ~ 10.0.0.254
```
-> 총 254개 (실제로 일반적인 장치에 할당할 수 있는 IP는 254개이다)



#### Subnetting
- 하나의 네트워크를 여러 개의 작은 네트워크로 나눌 수 있음
`/24`에서는 전체 사용가능한 주소가 256개이다.
그런데 회사에 컴퓨터가 50대라면 254개의 IP를 전부 하나의 네트워크로 사용하는 게 비효율적이다!
-> 그래서 여러 개의 작은 네트워크로 나눔(Subnetting)

예시) `/24`를 `/26`으로 바꾸기
-> 네트워크 비트가 26개란 뜻이고, 호스트 비트는 6개가 됨
-> 2의 6승 = 64개
-> 192.168.1.0을 64개로 쪼개면 
    ```
    192.168.1.0   ~ 192.168.1.63
    192.168.1.64  ~ 192.168.1.127
    192.168.1.128 ~ 192.168.1.191
    192.168.1.192 ~ 192.168.1.255
    ```
    총 4개의 네트워크가 됨

-> 첫 번째 서브넷:
    - 범위: `192.168.1.0 ~ 192.168.1.63`
    - 네트워크 주소: `192.168.1.0`
    - 브로드캐스트 주소: `192.168.1.63`

->따라서 사용 가능한 IP는 총 **62**개임

### 라우터와 게이트웨어
#### 라우터
- 라우터: 서로 다른 네트워크를 연결해주는 장비

예를 들어:
```
네트워크 A                 네트워크 B

192.168.1.0/24             10.0.0.0/24
     │                           │
   PC A                        서버
     │                           │
     └─────── [라우터] ─────────┘
```
PC A와 서버는 서로 다른 네트워크에 있습니다.

이 둘이 통신하려면 중간에 라우터가 필요합니다.

#### 스위치와 라우터의 차이
- 스위치: 같은 네트워크 안에서 장치들을 연결
```
PC ─┐
PC ─┼─ [스위치]
PC ─┘
```
MAC 주소를 이용해 데이터를 전달함

- 라우터: 서로 다른 네트워크 사이를 연결
```
네트워크 A
    │
 [라우터]
    │
네트워크 B
```
IP 주소를 보고 어디로 보낼지 결정

#### 게이트웨이
- 게이트웨이: 자신이 속한 네트워크 밖으로 데이터를 보내야 할 때 사용하는 출구
```
내 PC
192.168.1.10
      │
      ▼
기본 게이트웨이
192.168.1.1
      │
      ▼
인터넷
```

#### 게이트웨이와 라우터의 차이
Router = 네트워크와 네트워크를 연결하고 데이터를 어느 쪽으로 보낼지 결정하는 장비
Gateway = 내 네트워크에서 다른 네트워크로 나갈 때 사용하는 출구

즉 Router는 장비/기능의 개념, Gateway는 출구의 개념입니다.


### 실제 흐름
```
[내 PC]
192.168.1.10/24
      │
      │ 목적지: 10.0.0.10
      ▼
[기본 Gateway]
192.168.1.1
      │
      ▼
[Router]
      │
      ▼
[Router]
      │
      ▼
[인터넷]
      │
      ▼
[서버 네트워크]
10.0.0.0/24
      │
      ▼
[서버]
10.0.0.10
```
- 실제 인터넷에서는 중간에 여러 라우터가 있을 수 있음
- 내 PC가 Gateway로 보내야 한다면 앞에서 보인 MAC 주소가 쓰임

