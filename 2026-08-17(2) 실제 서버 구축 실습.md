# 1. VM 만들기
(1) CPU / RAM 설정
예시:
```
CPU : 2 vCPU
RAM : 4 GB
```

- CPU: 계산 담당
- RAM: 저장 담당(임시)

(2) OS 선택
예시:
```
Ubuntu
Rocky Linux
CentOS 계열
Windows Server
```

(3) Disk 
- 서버의 저장 공간

(4) Network
- VM에 네트워크에 연결되어야 함

(5) IP 설정
VM이 네트워크에서 사용할 IP가 필요함

- 내부 네트워크에서 사용하는 주소
```
Private IP
10.0.1.10
```

- 인터넷에 있는 내 PC에서 서버에 접속하려면 Public IP가 필요함
```
내 PC
 ↓
203.0.113.10
 ↓
Linux VM
```

(6) Firewall
Public IP가 있다고 해서 아무나 서버에 들어오면 위험함!


# 2. Linux 서버 운영
-서버 관리자가 된 것처럼 서버 상태를 확인하기

|명령어|설명|
|ip|네트워크 정보를 확인할 때 사용|
|ping|다른 장비와 네트워크 연결이 되는지 확인할 때 사용|
|curl|서버에 HTTP 요청을 보내거나 URL 응답을 확인할 때 사용|
|ps|현재 실행되고 있는 프로세스를 확인|
|df|Disk의 사용량을 확인|
|du|특정 디렉터리나 파일이 얼마나 많은 Disk 공간을 사용하는지 확인할 때 사용|
|free|RAM 상태를 확인|
|systemctl|서비스를 관리하는 명령어|
|journalctl|로그를 확인|


# 3. 직접 Web Server 띄우기
