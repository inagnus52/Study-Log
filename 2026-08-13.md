# 2026-08-13 목요일
## 오늘 배울 내용
브라우저에서 웹사이트 하나를 접속했을 때 내부에서 일어나는 일을 설명할 수 있다.
**TCP/IP**
- OSI 7 Layer
- TCP/IP 4 Layer
- TCP
- UDP
- 3-way handshake
- 패킷
- Port

**DNS + HTTP/HTTPS**
- DNS
- Domain
- HTTP
- HTTPS
- GET
- POST
- Response
- Status Code
- 200 / 301 / 404 / 500


## 오늘 배운 내용
### TCP/IP
#### OSI 7계층
```
7  응용 계층       HTTP, DNS
6  표현 계층
5  세션 계층
4  전송 계층       TCP, UDP
3  네트워크 계층   IP
2  데이터링크 계층 MAC
1  물리 계층       케이블, 전파
```

지금 배운 것과 연결하면:
```
MAC 주소 → 2계층
IP 주소  → 3계층
TCP/UDP  → 4계층
HTTP     → 7계층
```

#### TCP/IP 4계층
- 실제 인터넷에서는 TPC/IP 모델을 더 많이 사용함
```
응용 계층
   ↓
전송 계층
   ↓
인터넷 계층
   ↓
네트워크 접근 계층
```
대표적으로:
| 계층      | 대표적인 것           |
| ------- | ---------------- |
| 응용      | HTTP, HTTPS, DNS |
| 전송      | TCP, UDP         |
| 인터넷     | IP               |
| 네트워크 접근 | Ethernet, MAC    |

### TCP와 UDP
#### TCP
- TCP: 데이터를 안전하게 주고받기 위한 통신 방법
    예를 들어 100개의 데이터를 보냈다고 합시다.

    TCP는 상대방에게 데이터가 제대로 도착했는지 확인하고, 문제가 있으면 다시 보내는 등의 작업을 합니다.

    그래서 TCP는 신뢰성이 중요할 때 사용합니다.

#### UDP
    - TCP와 반대되는 느낌으로 이해하면 쉬움
    - "일단 빠르게 보내자"

    TCP처럼 복잡하게 연결하고 확인하는 과정이 적습니다.
    그래서 빠른 통신이 중요한 경우 사용합니다.

### TCP 3-way handshake
```
Client                 Server

   SYN  ───────────────→
        ←──────── SYN+ACK
   ACK  ───────────────→

        연결 완료
```

### IP와 Port
- IP = 건물 주소
- Port = 건물 안의 특정 문
   | Port | 용도         |
   | ---: | ---------- |
   |   80 | HTTP       |
   |  443 | HTTPS      |
   |   22 | SSH        |
   |   53 | DNS        |
   | 3306 | MySQL      |
   | 5432 | PostgreSQL |

### DNS + HTTP/HTTPS
브라우저에 `www.example.com`을 입력했을 때 서버까지 어떻게 찾아가는가?

#### DNS
- Domain(도메인)
- DNS = Domain Name System
   - 도메인 이름을 IP 주소로 찾아주는 시스템

#### 실제 웹페이지에 접속하는 과정
```
도메인   https://www.example.com
 ↓
DNS
 ↓
IP 주소  93.184.216.34
 ↓
TCP
 ↓
HTTPS
 ↓
웹 서버
```

#### 실제 HTTP 요청
**1. Get 요청**
브라우저가 서버에게 `example.com/index.html` 을 요청하면 실제로는 대략 
```
Get /index.html HTTP/1.1
Host: example.com
```
이런 형태의 HTTP 요청이 가게 된다

GET: 데이터를 달라는 뜻
/index.html: 원하는 리소스
HTTP/1.1: 사용하는 HTTP 버전
Host: example.com: 어느 웹사이트에 요청하는지 알려줌

**2. 서버가 Response를 보냄**
```
HTTP/1.1 200 OK

<html>
    ...
</html>
```
HTTP/1.1: HTTP 버전
200 ok: 요청 성공
아래의 HTML: 실제 웹페이지 데이터

브라우저는 HTML을 받아서 화면에 웹페이지를 그림(HTML 뿐만 아니라 CSS, JS, 이미지, 폰트 등도 같이 받음 -> 웹페이지 하나를 열면 수많은 HTTP 요청이 발생함)



#### 최종 정리
```
[사용자]

   https://www.example.com
              │
              ▼
          [DNS]
              │
              ▼
         [IP 주소]
              │
              ▼
         [Port 443]
              │
              ▼
      [TCP 3-way handshake]
              │
              ▼
           [HTTPS]
              │
              ▼
        [HTTP GET 요청]
              │
              ▼
          [웹 서버]
              │
              ▼
       [HTTP Response]
              │
              ▼
          [200 OK]
              │
              ▼
      [HTML / CSS / JS]
              │
              ▼
        [브라우저 화면]
```