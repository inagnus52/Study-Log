# 2026-07-25(토)
## 오늘 배울 내용
- 리눅스 디렉터리 구조
- 절대경로와 상대경로 
- /, /home, /etc, /var, /tmp, /usr 
- 파일과 디렉터리 차이
- 명령어 실습

- 파일 내용 확인
- 텍스트 검색
- 출력 리다이렉션
- 파이프 사용
- 명령어 실습

## 오늘 배운 내용
#### 1. 리눅스 디렉토리 구조
---
- 리눅스의 모든 파일과 디렉토리는 '/' 에서 시작한다
```/
├── home/
├── etc/
├── var/
├── tmp/
└── usr/
```

(- Windows는 C:\, D:\ 드라이브별로 나뉨)

| 디렉터리    | 의미               |
| ------- | ---------------- |
| `/`     | 리눅스 전체 디렉터리의 시작점 |
| `/home` | 일반 사용자의 개인 디렉터리  |
| `/etc`  | 프로그램과 시스템의 설정 파일 |
| `/var`  | 로그처럼 계속 변경되는 데이터 |
| `/tmp`  | 임시 파일            |
| `/usr`  | 프로그램과 명령어, 라이브러리 |

#### 2. 절대경로와 상대경로
--- 
- 절대경로: `\`부터 목적지까지 전부 작성한 경로
```/home/sunga/cloud-study/linux```

-현재 어디에 있든 항상 같은 위치를 가리킴

- 상대경로: 현재 위치를 기준으로 작성한 경로
| 표현   | 의미             |
| ---- | -------------- |
| `.`  | 현재 디렉터리        |
| `..` | 상위 디렉터리        |
| `~`  | 현재 사용자의 홈 디렉터리 |

예시:
```cd ..
cd ~
cd .
```

#### 3. 파일과 디렉터리 차이
---
- **파일**: 실제 내용을 저장
```commands.txt
memo.txt
photo.jpg
program.py
```

- **디렉터리** : 파일이나 다른 디렉터리를 정리해서 담는 공간(Windows의 폴더와 거의 같은 개념)

- `ls -l` 명령어: 이름만 보고 파일인지 디렉터리인지 확실하지 않을 때 사용하는 명령어

    결과가

    - `d` -> 디렉터리

    - `-` -> 일반 파일


---
#### 첫 번째 실습
```
cd ~    # 개인 홈 디렉터리로 이동
pwd     # 현재 위치 확인
ls      # 현재 위치의 파일 목록 확인
ls -al  # 숨김 파일을 포함해 자세히 확인
```

#### 4. /, /home, /etc, /var, /tmp, /usr
---
- 리눅스의 기본 뼈대가 되는 디렉터리들
- `/`: 리눅스 전체 디렉터리의 시작점 
    -모든 것이 `/` 아래에 있음
    ```
    cd /
    ls
    ```
- `/home`: 일반 사용자들의 개인 공간
- `/etc` : 시스템과 프로그램의 설정 파일이 들어 있음
- `/var`: 계속 변하는 데이터가 저장되는 곳
    주로 다음이 들어 있음
    - 로그 파일
    - 캐시
    - 웹 서버 데이터
    - 프로그램 실행 중 생성되는 데이터
- `/tmp`: 임시 파일을 저장하는 디렉터리(잠깐 사용하는 파일이나 테스트용 파일)
    - 중요한 파일을 저장하면 안됨!
- `/usr`: 사용자가 사용하는 프로그램, 명령어, 라이브러리 등이 들어 있음

| 디렉터리    | 역할             |
| ------- | -------------- |
| `/`     | 리눅스 전체의 시작점    |
| `/home` | 사용자 개인 파일      |
| `/etc`  | 시스템 설정 파일      |
| `/var`  | 로그와 계속 변하는 데이터 |
| `/tmp`  | 임시 파일          |
| `/usr`  | 프로그램과 라이브러리    |

#### **실습**
```
cd /    # '/' 디렉터리로 이동 
pwd     # 지금 내가 어디에 있는지 확인
ls      # 현재 디렉터리 안의 파일과 디렉터리 목록 보기

cd /home
pwd
ls
# 결과: 
parkseonga@DESKTOP-4CLG47Q:/home$ ls
parkseonga  
# /home 디렉터리 안을 확인했더니, 그 안에 사용자 개인 디렉터리인 parkseonga 라는 디렉터리가 하나 있다는 뜻
# 내 개인 홈 디렉터리로 이동하려면 `cd parkseonga`

cd /etc
pwd
ls

cd /var
pwd
ls

cd /tmp
pwd
ls

cd /usr
pwd
ls
```

#### 5. 파일 내용 확인
---
-파일 안에 어떤 내용이 들어 있는지 보는 기능
-대표 명령어
```
cat
less
head
tail
```
- **`cat`**: 파일의 전체 내용을 터미널에 한 번에 보여줌
![cat 명령어 실습](./images/cat%20명령어%20실습.png)
-cat은 파일의 내용을 보여 달라는 뜻

- **`less`**: `cat`은 파일 전체를 한 번에 보여주지만, `less`는 파일이 길 때 화면을 넘기면서 볼 수 있음
    - `↑`, `↓` : 한 줄씩 이동
    - `Space` : 다음 화면
    - `b` : 이전 화면
    - `q` : 종료

- **`head`**: 파일 앞부분 보기
    -기본적으로 파일의 처음 10줄을 보여줌
    -`-n`을 사용하면 특정 줄 수만 볼 수 있음
    ``` head -n 1 test.txt ```

- **`tail`**: 파일의 마지막 부분 보기
    -기본적으로 파일의 마지막 10줄을 보여줌
    -`-n`을 사용하면 특정 줄 수만 볼 수 있음
    ``` tail -n 1 test.txt```

#### 6. 텍스트 검색
- **`grep`**: 파일 안에서 특정 단어 찾기
![grep 명령어 실습](./images/grep%20명령어%20실습.png)

#### 7. 출력 리다이렉션
- **`>`**: 출력 결과를 파일에 저장
```echo "첫 번째 내용" > result.txt```
    뜻은:
    - `echo "첫 번째 내용"`: 화면에 글자를 출력
    - `>`: 그 출력 결과를 파일로 보냄
    - **`>`를 다시 쓰면 기존 내용을 지우고 덮어씀**

-**>>**: 기존 파일 뒤에 내용 추가
```echo "추가 내용" >> result.txt```
    뜻은: 
    - 기존 result.txt 내용을 지우지 않고
    - 맨 아래에 "추가 내용"을 붙입니다.

#### 8. 파이프 `|`
- `|`: 앞 명령어의 결과를 뒤 명령어로 전달
```cat test.txt | grep "리눅스"```
    뜻은:
    - cat test.txt가 파일 전체 내용을 출력하고
    - |가 그 결과를 grep으로 넘기고
    - grep "리눅스"가 리눅스가 들어간 줄만 보여줍니다.

#### 실습: 명령어만 사용해 디렉터리 만들기
![commands.txt 파일 실습](./images/commands.txt%20파일%20실습.png)
![commands.txt 파일 실습2](/images/commands.txt%20파일%20실습2.png)