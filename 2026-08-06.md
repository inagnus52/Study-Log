# 2026-08-06 목요일
## 오늘 배울 내용
- Ubuntu에 SSH 서버 설치 및 실행
- Windows에서 Ubuntu로 접속
- 종료 후 재접속
- scp로 파일 전송
- SSH 키 생성 및 키 접속
- IP·포트·프로토콜·SSH 관계 정리

## 오늘 배운 내용
### Windows에서 Ubuntu로 접속
**step 1. SSH 서버 설치하기**

**step 2. Windows에서 Ubuntu까지 SSH로 접속**
**Windows PowerShell을 열고:**
```
ssh parkseonga@Ubuntu 주소
```
명령어 실행

- 접속에 성공하면 프롬프트가 Ubuntu 사용자 계정으로 바뀜
```
parkseonga@DESKTOP-4CLG47Q:~$
```
- `who` 명령어로 SSH로 접속한 상태인지 확인할 수 있음
- `exit` 명령어로 종료

### scp로 파일 전송
```
scp 보낼파일 사용자명@서버IP:저장경로
```
- `scp`는 SSH 연결을 이용해 파일을 안전하게 복사하는 명령어이다

![scp로 파일 전송 실습](/images/scp로%20파일%20전송하기%20실습.png)

+) 파일 전송 후 SSH로 접속하여 확인하기

### SSH 키 접속
**키 인증이란???????**
- 키 인증은 쉽게 말해서 비밀번호 대신 열쇠로 나라는 것을 증명하는 로그인 방식!!
1. Windows가 Ubuntu에 SSH 접속을 요청함
2. Ubuntu는 갖고 있는 공개 키로 "개인 키 있나요" 확인함
3. Windows는 개인 키를 가지고 있다는 증거를 보여줌(개인 키 보여주는 거 아님)
4. Ubuntu가 증거랑 공개 키를 대봐서 검증함
5. 검증에 성공하면 비밀번호가 없어도 접속시켜 줌


**키 만들기**
```
ssh-keygen -t ed25519
```
![키 생성 실습](/images/키%20생성%20실습.png)
- `id_ed25519`: 개인 키(Windows에서만 보관하고 절대 공유 x)
- `id_ed25519.pub`: 공개 키(Ubuntu 서버에 등록하는 키)
- 완료 후 키가 생성됐는지 확인(`dir HOME\.ssh`)

**만든 공개 키를 Ubuntu에 등록하기**
```
Get-Content $HOME\.ssh\id_ed25519.pub | ssh parkseonga@172.30.246.174 "mkdir -p ~/.ssh; cat >> ~/.ssh/authorized_keys; chmod 700 ~/.ssh; chmod 600 ~/.ssh/authorized_keys"
```

**SSH로 접속해보기**
```
ssh parkseonga@172.30.246.174
```
- 비밀번호를 묻지 않고 바로 접속되면 SSH 키가 인증된것임