# 2026-08-15 토요일
## 오늘 배울 내용
**서버 인프라 구조**
- Web Server
- WAS
- Database
- Reverse Proxy
- Load Balancer

**서비스 아키텍처**
- 확장성
- 장애
- 부하
- 이중화
- 가용성

"왜 클라우드에서 서버를 여러 개 만들고 로드밸런서를 사용하는가?"

## 오늘 배운 내용
### 서버는 혼자 일하지 않는다
```
사용자
  ↓
Web Server
  ↓
WAS
  ↓
Database
```
- Web Server: 정적인 것을 담담
- WAS: 프로그램을 실행해서 동적인 결과를 만들어냄
- Database

->**하는 일이 다른 서버를 여러 개 만드는 것!**

#### Web Server
HTML, CSS, JavaScript, 이미지 같은 정적 파일을 제공하는 역할
- Nginx
- Apache
등

#### WAS
Web Application Server
프로그램을 실행하는 역할, 애플리케이션의 로직을 실행하는 곳
- Tomcat
- Jetty
등이 있음

예) 사용자가 '장바구니' 조회를 요청하면 WAS가 DB에서 사용자의 장바구니를 조회해서 응답함

#### Database
데이터를 저장하는 곳/조회

#### Proxy
중간에서 대신 요청을 전달해주는 서버
```
클라이언트 ───→ Proxy ───→ 서버
```
---
웹사이트 하나를 운영하기 위해서 서버가 여러 대일 수 있음(사용자가 보기에는 하나의 웹사이트처럼 보여도, 뒤에서는 여러 서버가 일을 나눠서 한다고 생각하면 됨!)
    - Web Server, WAS, Database, 기타 서버 등등
    - WAS가 여러 대일 수도 있음!

여러 서버 중에 

+)그리고 서버는 컴퓨터 한 대 일수도, 가상 서버(VM)일수도 있음

왜 중간에 Proxy를 넣을까?
-보안, 캐싱, 접근제어, 로드밸런싱, 서버 보호, IP 숨기기, 여러 서버로 요청 분배 등의 여러 이유가 있음

```
                        ┌→ Web Server 1
사용자 → Reverse Proxy   ──→ Web Server 2
                        └→ Web Server 3
```
이런 상황일 때 서버를 하나 골라서 요청을 전달함
한 서버에 전부 몰리는 대신 분산할 수 있는데 이걸 **Load Balancing(로드 밸런싱)**이라고 함

---
```
사용자
  ↓
Reverse Proxy
  ↓
Web Server
```
이런 상황에서 Proxy를 쓰는 이유는 사용자가 Web Server의 존재나 내부 구조를 다 알 필요가 없음.

혹은

```
사용자
  ↓
www.example.com
  ↓
Reverse Proxy
  ↓
10.0.0.11
```
사용자는 `10.0.0.11`이라는 내부 서버의 IP를 직접 알 필요가 없음


혹은

```
                 ┌→ Web Server 1
인터넷 → Reverse Proxy ─→ Web Server 2
                 └→ Web Server 3
```
사용자가 인터넷에서 웹사이트에 접속할 때, Web Server 1, 2, 3 중 어떤 서버의 IP를 몰라도 됨


---
**IP와 Port와 Proxy**
```
사용자
  ↓
Reverse Proxy
  ↓
10.0.0.12:80
  ↓
Web Server 2
```
사용자의 요청을 Reverse Proxy가 서버로 전달할 때, IP로 서버를 찾고, Port로 그 서버의 특정 서비스에 연결한다
```
사용자
  ↓
Reverse Proxy
  ↓
10.0.0.12:80
  ↓
Web Server 2
```
---

- **Reverse Proxy vs Forward Proxy**

|Forward Proxy|Reverse Proxy|
|사용자를 대신해서 서버에 요청|서버를 대신해서 요청을 받음|
|사용자->Proxy->인터넷|인터넷->Proxy->서버|
|직원pc->회사 Proxy->인터넷|인터넷 사용자->Reverse Proxy->내부 서버|

---
**총정리**
```
[사용자 측]                    [서버 측]

사용자 → Forward Proxy → 인터넷 → Reverse Proxy → Web Server
          ↑                            ↑
       사용자 대신                  서버 대신
```

### 서비스 아키텍처
서버가 하나인 쇼핑몰은 할인 이벤트를 시작해서 사용자가 몰리면 서버가 처리해야 할 요청이 많아졌을 때, 서버의 CPU나 메모리가 부족해줄 수 있다

그래서 서버를 여러 대 만든다(Load Balancing)

- 장애
- 이중화: 하나가 고장 나도 다른 것이 대신 처리할 수 있게 하는 것
- 부하: 서버가 처리해야 하는 일이 많아지는 것
- 확장성: 사용자가 늘어나면 서버를 더 추가해서 처리 능력을 늘리는 것( = Scale-out, 수평 확장)
- 가용성: 서비스를 얼마나 끊김 없이 사용할 수 있는가
