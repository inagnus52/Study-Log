# 2026-08-09 일요일
## 오늘 배울 내용
**「컴퓨터 → 서버 → 인프라」**
- CPU
- RAM
- SSD/HDD
- NIC
- 메인보드
- 운영체제
- 프로그램
- 프로세스
- 서버
- 클라이언트
- 데이터센터

**「운영체제와 Linux」**

- OS란?
- Windows vs Linux
- Kernel
- 프로세스
- 서비스
- 파일 시스템
- CLI vs GUI
- 왜 서버에서 Linux를 많이 쓰는가?

## 오늘 배운 내용
### 컴퓨터 기초
- 컴퓨터가 일을 하는 구조
```
             컴퓨터
                │
      ┌─────────┼─────────┐
      ↓         ↓         ↓
     CPU       RAM       Disk
      │         │         │
   계산/처리    작업공간    저장공간
```
(책장 = 디스크
책상 = RAM
나 = CPU)

- `CPU`: Central Processing Unit
    - 계산과 명령 담당임
    - `123 x 456`과 같은 산수뿐만 아니라 서버에서도 똑같음
    ```
    아이디/비밀번호 입력
        ↓
    서버가 요청 받음
        ↓
    CPU가 프로그램의 명령 실행
        ↓
    DB에서 정보 확인
        ↓
    결과 반환
    ```

- `RAM`: 작업 공간이라고 생각하면 쉬움
    - 디스크라는 책장에서 책을 꺼내 RAM이라는 책상에서 펼쳐놓고 내(CPU)가 공부함
    - 따라서 RAM이 부족하면 동시에 많은 프로그램을 처리하기 어려움

- `Disk`: 데이터를 오래 보관하는 저장공간
    - HDD, SSD가 대표적
    - 컴퓨터를 껐다 켜도 사진이 남아있는 이유가 디스크에 저장되어 있기 때문

|        | RAM        | SSD/HDD    |
| ------ | ---------- | ---------- |
| 역할     | 작업 공간      | 저장 공간      |
| 속도     | 빠름         | 상대적으로 느림   |
| 전원 OFF | 내용 사라짐(휘발성)| 데이터 유지(비휘발성)|
| 예      | 실행 중인 프로그램 | 파일, OS, 사진 |

- `OS(운영체제, Operating System)`: 컴퓨터의 하드웨어와 프로그램 사이를 관리해주는 기본 소프트웨어
    예를 들어 프로그램이 CPU를 사용하고 싶다고 해서 CPU에 직접 명령을 내리는 식으로 작동하지 않음
    ```
    사용자 -> 프로그램 -> OS -> CPU, RAM, Disk
    ```

- 프로그램과 프로세스는 다르다
    - 프로그램 = 실행할 수 있도록 저장되어 있는 것
    - 프로세스 = 실제로 실행되고 있는 것


## 서버
- 서버도 기본적으로 컴퓨터임
```
내 PC -> 내가 사용
서버 -> 다른 컴퓨터에게 서비스를 제공
```

- Client: 서비스를 요청하는 쪽
- Server: 서비스를 제공하는 쪽

- 서버의 종류
    - Web Server: 웹페이지를 제공
    - WAS: 웹 애플리케이션을 실행
    - DB Server: 데이터를 저장하고 관리
    - DNS Server: 도메인 이름과 IP 주소를 연결
    - Mail Server: 메일 서비스를 제공

    - 이런 서버들이 서로 연결됨

---
- 서버는 24시간 켜져있음
    - 서버는 일반 PC와 달리 24시간 운영, 높은 안정성, 네트워크 연결, 장애 대응, 백업, 모니터링이 중요하므로 항상 켜져있음
    
- **데이터센터**: 서버가 위치하는 곳
```
┌───────────────────────────┐
│       데이터센터           │
│                           │
│  ┌────┐ ┌────┐ ┌────┐     │
│  │서버│ │서버│ │서버│ ... │
│  └────┘ └────┘ └────┘     │
│                           │
│   네트워크 / 전력 / 냉각    │
└───────────────────────────┘
```
-> 클라우드는 결국 이런 물리적인 인프라를 기반으로 만들어지기 때문에 앞으로 수업 때 꼭 필요한 지식들임!


## Linux와 운영체제
컴퓨터를 키면:
```
전원 -> Windows -> 바탕화면 -> 프로그램 실행
```
컴퓨터를 관리하는 운영체제인 Windows가 있다.

Linux도 운영체제의 일종임
    -정확히 말하면 Linux는 원래 커널을 가리키는 말이지만, 실무에서는 Ubuntu, Rocky Linux, Debian 같은 배포판을 편하게 Linux라고 부른다

- 왜 서버에서 Linux를 많이 쓰는가
    ① 안정성    
    서버는 오랫동안 계속 실행되어야 합니다.

    ② 가볍게 운영 가능
    GUI 없이 필요한 기능만 실행할 수 있습니다.

    ③ 자동화가 편함
    명령어와 스크립트를 이용해 서버 작업을 자동화하기 좋습니다.

    ④ 서버용 소프트웨어가 많음
    웹 서버, DB, 컨테이너 등 다양한 서버 프로그램이 Linux 환경에서 널리 사용됩니다.

    ⑤ 비용과 라이선스 측면
    많은 Linux 배포판이 오픈소스 생태계를 기반으로 합니다.

- GUI와 CLI
    - GUI(Graphical User Interface): 마우스와 창을 이용함
    - CLI(Command Line Interface): GUI 없이 명령어로 작업
    - 서버에 직접 모니터와 마우스를 붙여놓고 관리하는 것이 아니라 네트워크를 통해 원격으로 접속해서 관리하는 경우가 많기 때문에 서버에서는 CLI가 굉장히 중요하다

- Terminal과 Shell
    - Terminal: 명령어를 입력할 수 있는 환경
    - Shell: 사용자가 입력한 명령을 해석해서 운영체제에 전달하는 프로그램
    ```
    사용자
    ↓
    Terminal
    ↓
    Shell
    ↓
    Linux Kernel
    ↓
    하드웨어
    ```

- Kernel
    - 운영체제의 핵심 부분
    ```
    ┌───────────────────────┐
    │      사용자/프로그램   │
    ├───────────────────────┤
    │         OS            │
    │       Kernel          │
    ├───────────────────────┤
    │ CPU / RAM / Disk / NIC │
    └───────────────────────┘
    ```
    - 프로그램이 CPU나 메모리 등을 사용하려면 Kernel이 관리해줌
    - 컴퓨터 자원을 관리하는 핵심 관리자
---
### Linux 파일 시스템
- **root directory**
    가장 위에 있는 파일 시스템으로 **`\`**으로 나타냄
    ```
    /
    ├── bin
    ├── etc
    ├── home
    ├── root
    ├── tmp
    ├── usr
    └── var
    ```

| 경로      | 의미             |
| ------- | -------------- |
| `/`     | 최상위 디렉터리       |
| `/home` | 일반 사용자 홈       |
| `/root` | root 사용자 홈     |
| `/etc`  | 설정 파일          |
| `/var`  | 변경되는 데이터, 로그 등 |
| `/tmp`  | 임시 파일          |
| `/usr`  | 프로그램/시스템 관련 파일 |


---

서버에 문제가 생겼다. "디스트 용량이 부족합니다"
그러면 서버에 접속해서 `df -h` 같은 명령어로 디스트 상태를 확인할 수 있어야 한다.
"누가 CPU를 많이 사용하고 있지?" -> `top` 명령어

