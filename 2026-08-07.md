# 2026-08-08 토요일
## 오늘 배울 내용
- 디스크와 파티션 
- 파일시스템 
- 마운트 
- NAS와 로컬 디스크 차이 
- 블록 스토리지와 파일 스토리지 기초
- 파일 압축과 해제 
- 디렉터리 백업 파일 만들기 
- 환경변수 
- PATH 
- 셸 변수 
- 간단한 Bash 스크립트

## 오늘 배운 내용
### 디스크와 파티션
- **디스크**: 데이터를 저장하는 물리적 또는 가상 저장장치 (HDD, SSD, 가상 디스크 등이 해당)
- **파티션**: 하나의 디스크를 용도에 따라 나눈 구역
- **파일시스템**: 파티션에 파일과 디렉터리를 저장하고 관리하는 방식
- **마운트**: 파일시스템을 리눅스의 특정 디렉터리에 연결하는 작업
```
디스크 → 파티션 생성 → 파일시스템 생성 → 디렉터리에 마운트 → 파일 저장
```

#### 저장장치 구조 확인 명령어 실습
```
lsblk
```
- list block devices, 디스크와 파티션 같은 블록 장치를 목록으로 보여주는 명령어

![저장장치 구조 확인 명령어 실습](/images/저장구조%20확인%20명령어%20실습.png)
각 열의 의미입니다.

- `NAME`: 장치 이름
- `MAJ:MIN`: 커널이 장치를 식별하는 번호
- `RM`: 분리 가능한 장치인지 표시합니다. 0이면 일반적으로 고정 장치입니다.
- `SIZE`: 장치 용량
- `RO`: 읽기 전용 여부입니다. 1이면 읽기만 가능하고, 0이면 쓰기도 가능합니다.
- `TYPE`: 장치 종류입니다. 모두 disk로 표시되어 있습니다.
- `MOUNTPOINTS`: 파일시스템이 연결된 위치입니다.
- `[SWAP]`: 일반 디렉터리가 아니라 스왑(메모리가 부족할 때 보조로 사용하는 공간)으로 사용 중이라는 의미입니다.


**전체 파일시스템의 용량과 사용량 확인하는 명령어 실습**
```
df -h
```
- `df`는 disk free
- `-h`는 사람이 읽기 편하게 GB와 MB 단위로 표시한다는 뜻

```
du -sh ~
```
- `du`: 파일이나 디렉터리가 차지하는 용량
- `-s`: 하위 내용을 합한 총합만 표시
- `-h`: 읽기 쉬운 단위로 표시
- `~`: 현재 사용자의 홈 디렉터리 (/home/parkseonga)

![전체 파일시스템의 용량과 사용량 확인하는 명령어 실습](/images/전체%20파일시스템의%20용량과%20사용량%20확인하는%20명령어%20실습.png)



**Windows의 C드라이브가 Ubuntu에 마운트되어 있는 경로**
`C:\`
```
C:\  460G  314G  147G  69%  /mnt/c
```

Windows의 C 드라이브가 Ubuntu의 /mnt/c에 연결되어 있습니다. 따라서 Ubuntu에서 다음 경로로 이동하면 Windows 파일에 접근할 수 있습니다.

cd /mnt/c

예를 들어 Windows 사용자 폴더는 다음 위치에 있습니다.

cd /mnt/c/Users

drivers도 C 드라이브와 동일한 용량과 사용량을 보여주는데, WSL이 Windows 드라이버 파일에 접근하기 위해 별도로 연결한 항목입니다.


### NAS와 로컬 디스크
- 로컬 디스크: 현재 컴퓨터 안에 직접 장착되거나 가상으로 연결된 저장장치
- NAS: 네트워크를 통해 여러 기기가 함께 사용하는 별도의 네트워크 저장장치

### 블록 스토리지와 파일 스토리지
(목적에 따라 저장공간을 골라쓰는 것임)
||사용 상황|차이점|
|---|---|---|
|블록 스토리지|서버에 전용 디스크 공간이 필요할 때|컴퓨터에 새 SSD를 장착|
|파일 스토리지|여러 컴퓨터나 서버가 같은 파일을 공유해야 할 때|네트워크에 있는 공용 폴더를 사용|
- 핵심: 블록 스토리지는 디스크를 빌리는 것이고, 파일 스토리지는 폴더를 빌림


| 필요한 상황                   | 선택      |
| ------------------------ | ------- |
| 서버 운영체제를 설치해야 함          | 블록 스토리지 |
| 데이터베이스를 저장해야 함           | 블록 스토리지 |
| 서버 디스크 용량을 추가해야 함        | 블록 스토리지 |
| 직원들이 같은 문서를 써야 함         | 파일 스토리지 |
| 여러 서버가 같은 이미지 파일을 사용해야 함 | 파일 스토리지 |
| 여러 기기에서 사진을 보관하고 싶음      | 파일 스토리지 |


**블록 스토리지**
- 운영체제에 디스크 하나를 제공하는 방식
- 새 SSD를 장착하면 바로 파일로 넣는 것이 아님
```
디스크 준비 → 파일시스템 생성 → 마운트 → 파일 저장
```
예시)
```
/dev/sdd → ext4 → /
```
- `/dev/sdd`: 디스크
- `ext4` : 파일을 관리하는 방식
- `/`: 디스크가 연결된 디렉터리

<br>

---
### `mount`와 `findmnt`
- `mount`: 