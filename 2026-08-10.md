# 2026-08-10 월요일
## 오늘 배울 내용

## 오늘 배운 내용
### Linux 명령어 실습
![Linux 명령어 실습](/images/Linux%20명령어%20실습.png)
1. `pwd`
- Print Working Directory
- "현재 `/home/user` 디렉터리 안에 있다" 라는 뜻

2. `ls`
- "여기 뭐가 있는지"
- 현재 디렉터리에 있는 파일과 폴더를 보여줌

- `ls -l`는 조금 더 자세하게
    - 맨 앞의 `-`와 `d`
        - `-` = 파일
        - `d` = 디렉터리

3. `cd`
- Change Directory
- 디렉터리 이동하기
```
cd cloud-study
```
- cloud-study 디렉터리로 들어감
- `pwd` 입력하면 현재 위치가 바뀐 걸 확인할 수 있음


|cd ..|..은 부모 디렉터리|
|cd /|Linux의 최상위 디렉터리인 `/`로 이동|
|cd ~|현재 사용자의 홈 디렉터리로 이동|


4. `mkdir`
- 폴더 만들기

5. `touch` 
- 파일 만들기


### 절대 경로와 상대 경로
- 절대 경로 
```
/home/parkseonga/cloud-study
```

- 상대 경로: 현재 위치를 기준으로 표현
```
cloud-study
```

### Linux 명령어 실습(2)
6. `cp`
- 파일을 복사
```
cp server.txt backup.txt
```
하면 
```
server.txt
backup.txt
```
원본은 그대로 있고 복사본이 하나 추가됨

7. `mv`
- (1)이동
    ```
    mv server.txt /tmp/
    ```
- (2)이름 변경
    ```
    mv server.txt webserver.txt
    ```

8. `rm`
- 삭제

- `rmdir`: 빈 디렉터리 삭제
- `rm -r test`: 하위 내용까지 재귀적(-r)으로 처리한다는 뜻으로 실무에서 자주 쓰임

9. `cat`: 파일 내용 출력
```
cat server.txt
```
KT Cloud Server
Linux 
가 출력됨

10. `echo`
- 파일에 내용 넣기
- `echo "Hello Linux" > test.txt`: 기존 내용 **덮어씀**
- `echo "Hello Linux" >> test.txt`: 기존 내용 뒤에 **추가함**

11. `grep`
- 원하는 내용 찾기
```
grep "Error" server.log
```

12. `find`
- 파일 찾기
```
find . -name "server.log"
```
- `.`은 현재 디렉터리라는 뜻

### Linux 권한(Permission)과 사용자
#### 권한 확인
```
ls -l
```
- `r` = read
- `W` = write
- `x` = Execute

![권한 실습](/images/권한%20실습.png)
- 맨 앞 `d`와 `-`
    - `d`: 디렉터리
    - `-`: 파일

- 권한은 세 그룹으로 나뉨
    ```
    -rwxr-xr-x 
    ```
    1) `rwx`: owner(파일을 만든 사람)
    2) `r-x`: group
    3) `r-x`: others(나머지 사용자)

    - 디렉터리에서 `x`는 실행 가능이라는 뜻보다는 접근/통과할 수 있는 권한과 관련있음
    - 텍스트 파일은 굳이 프로그램처럼 실행할 필요가 없으므로 `rw-` 정도만 있으면 됨

- 숫자로 표현하기도 함
|r|4|
|w|2|
|x|1|

#### `chmod`: 권한을 변경하는 명령어
- change mode
```
chmod 755 test1.txt
```
    - Owner => 7
    - Group -> 5
    - Others -> 5
    라는 뜻

#### `root`
- root = 최고 권한을 가진 사용자
    - 시스템의 거의 모든 영역에 접근할 수 있음

- `sudo`: 일반 사용자가 관리자 권한으로 명령어 실행할 때 사용(맨 앞에 붙임)
```
sudo systemctl restart nginx
```


## 총정리
| 명령어     | 의미                      | 역할          |
| ------- | ----------------------- | ----------- |
| `pwd`   | Print Working Directory | 현재 위치       |
| `ls`    | List                    | 파일/폴더 목록    |
| `cd`    | Change Directory        | 디렉터리 이동     |
| `mkdir` | Make Directory          | 디렉터리 생성     |
| `touch` | Touch                   | 파일 생성/시간 갱신 |
| `cp`    | Copy                    | 복사          |
| `mv`    | Move                    | 이동/이름 변경    |
| `rm`    | Remove                  | 삭제          |
| `cat`   | Concatenate             | 파일 내용 출력    |
| `grep`  | 검색                      | 문자열 검색      |
| `find`  | 찾기                      | 파일/디렉터리 검색  |
